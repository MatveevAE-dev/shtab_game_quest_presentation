# Шаги слайдов (источник правды по механикам)

Единый список **текущих шагов на каждом слайде**. Реализация — `presentation/index.html` (сцены в массиве `SCENES`, контроллер `applyScene`).

## Правила ведения

- **При любом изменении механики/шагов слайда — сразу обновляем этот файл** (тот слайд, где менялось).
- **Перед задачей по слайду — читаем его шаги здесь**, чтобы понимать текущую механику.
- Слайды нумеруем 1-based по `SCENES` (совпадает со счётчиком «[N/17]»).
- Детализируем слайд при первом касании; пока не трогали — краткая текущая механика.

## Карта слайдов (SCENES — 17 слайдов)


| #   | id        | Название                                                 |
| --- | --------- | -------------------------------------------------------- |
| 1   | title     | Титул (волны + лого/титул)                               |
| 2   | splash    | Заставка (загрузка → квест-титул)                        |
| 3   | hero      | Знакомство с героем                                      |
| 4   | rules     | Правила игры (3 подслайда)                               |
| 5   | avatars   | Проводники — 4 аватара                                   |
| 6   | map       | Карта: объекты + маршруты → разведка → кафе              |
| 7   | mission1  | Миссия 1 — начать бизнес (Путилин)                       |
| 8   | quiz      | Квиз                                                     |
| 9   | mission1b | Миссия 1 — часть 2 (ЕКЦ)                                 |
| 10  | binoc     | Миссия 2 — найти деньги (ФСК, разведка → Банк, Купринов) |
| 11  | fsk       | ФСК — финансирование                                     |
| 12  | cafeopen  | Открытие кафе (ремонт → готово → опасность)              |
| 13  | danger    | Опасность — эскаватор/снос (разведка биноклем)           |
| 14  | mission3  | Миссия 3 — защита бизнеса                                |
| 15  | fpsp      | ФПСП — соц. предпринимательство                          |
| 16  | shtab     | Штаб — защита бизнеса                                    |
| 17  | finale    | Финал — бизнес работает                                  |


> В `SCENES` есть неиспользуемый код `hero2` (копия знакомства с мыслями-облаками), но в потоке слайдов его НЕТ.

### Сквозной ранг героя (`LEVEL_NAMES`, звёзды)

Каждая сцена стартует с ранга, которым закончилась предыдущая. `LEVEL_NAMES` = [0 Новый игрок · 1 Начинающий · 2 Действующий · 3 Продвинутый · 4 Опытный · 5 Московский предприниматель].


| Слайд       | Ранг           | Событие          |
| ----------- | -------------- | ---------------- |
| 6 map       | 0★ Новый игрок | без level-up     |
| 7 mission1  | 0★ → **1★**    | `runCafeLevelUp` |
| 9 mission1b | 1★ Начинающий  | без level-up     |
| 10 binoc    | 1★ Начинающий  | —                |
| 11 fsk      | 1★ → **2★**    | `runFskLevelUp`  |
| 12 cafeopen | 2★ Действующий | —                |
| 13 danger   | 2★ Действующий | —                |
|             |                |                  |


---

## 1. title — Титул

- Фон: `#ccanvas` — волновое поле (canvas) + `cover.png` (частицы) + оверлей `ccTitleArt` (лого-строка + титул `title-lockup.svg`, только на титуле).
- 1-й клик → **вуаль закрытия** `ccVeil` в чёрное (0.7с) → на чёрном **большая кнопка «Начать»** (`.cbtn.cc-big`, Hauss Wide, ~8cqh).
- Клик (кнопка/любой) → переход на `splash`. `←` — назад в исходное (волны + подсказка).

## 2. splash — Заставка

**Механика:** экран загрузки → квест-титул увеличением → уход с переносом титула через вуаль («титул переживает вуаль»).

**Загрузка** (`runLoading`, проигрывается **ВСЕГДА**, в т.ч. при возврате; **пропуск любым кликом/→**):
Элементы появляются **по-очереди с паузами** (не одновременно):

1. **Сразу** — полоса `loadFill` плавно **0→100% за ~4.5с** (`LOAD_DUR=4533`) + процент `loadPct`.
2. **Пауза ~700мс → лог** — 4 строки по-очереди (слот в оставшемся времени): текст + мигающие «…» → через ~0.7 слота **галка ✓ + стоп** (done). Строки: «Ищу идею» · «Нахожу консультанта» · «Определяю локации» · «Подбираю субсидии».
3. **Пауза ~1.55с → подпись** `loadCap` появляется и циклится каждые 1.5с: «Гружу» → «Гружу-гружу» → «Гружу, ещё чуть-чуть».
4. Бар 100% → `finishLoading`. *(Клик/→ до конца загрузки → `finishLoading` пропускает сразу.)*

**Титул** (`finishLoading`):
5. `splash.loaded` — плавно прячутся бар/процент/подпись/лог.
6. Через **450мс** — квест-титул `questTitle` «МОСКОВСКИЙ ПРЕДПРИНИМАТЕЛЬСКИЙ КВЕСТ / НА ОДНОЙ ВОЛНЕ 2026», строки всплывают увеличением (`titlePop`, шаг 1.9с).
7. После блока — впечатывание штампа **«Новый / импульс»** (`quest-stamp`, **2 строки**, по центру; двойная неон-рамка обёрнута вокруг 2-строчного блока).

**Выход** (клик/→ после титула, `splashLeave`):
8. Титул пиним по экранным координатам (fixed) + замораживаем строки/штамп → **переносим тот же узел** `questTitle` в оверлей `questCarry` (над сценами) — без прыжка.
9. Вуаль `splashVeil` сворачивается фокусом **в центр (50%,50%)**: `--r` 175%→0% → чёрный → переход на `hero`.
10. На `hero` при раскрытии титул `questCarry` **растворяется** (fade ~1.9с). Возврат — узел домой (`questHome`), пин снят.

## 3. hero — Знакомство с героем

> Внутренняя подготовка (не шаг): `buildHeroQuestions` строит DOM мыслей + `resetHeroText` чистит текст.

1. **Вуаль раскрытия** из фокуса **(46%, 38%)**: `--r` 6% → **108% за 2с** (ровно до края экрана, без захода за экран).
2. **Контур героя** `hero-outline` — show + glow **сразу** (без прорисовки сверху-вниз).
3. **На 2000мс** (конец раскрытия): печать имени **«Новый игрок»** в чипе `.herobox`, **45мс/символ**, без звёзд (`typeChipName`).
4. По завершении имени — выноска `heroLead` (линия + точка к герою).
5. Через **400мс** — мысли героя **печатаются по-очереди** (`typeThought`, белый Hauss + пульсирующее **золотое свечение** `thinkGlow`, 45мс/символ; следующая через 400мс после конца предыдущей). 6 мыслей: «С чего вообще начать?» · «Где взять деньги на старт?» · «Как выбрать нишу?» · «А вдруг ничего не получится?» · «Какие нужны документы?» · «Кто вообще мне поможет?».
6. **Выход** (клик/→) `heroLeave`: стоп печати → `collapsing` (скрыть имя/выноску/мысли) → вуаль сворачивается в фокус **Москва-Сити (92%, 24%)**, `--r`→0 за **3.4с** → переход на `rules`. Клик во время сворачивания → `heroFinish` сразу. **Титул «Правила игры» здесь НЕ показывается** — он появляется как вход на слайд 4.

## 4. rules — Правила игры

- **У КАЖДОГО подслайда СВОЙ левый титул** `#rulesTitle` (`.rules-title`, uppercase, слева, по центру; `white-space:normal` + `max-width:36cqw` — длинный переносится в 2 строки): `RULES_TITLES` = [0 «Правила игры» · 1 «Уровень сложности» · 2 «Тренды»]. Смена = `setRulesTitle(step)` — **скрыть мгновенно → сменить текст → плавно ПРОЯВИТЬ** (fade `.6s`); на 2-м/3-м подслайде титул проявляется при переходе.
- **Вход:** чёрное → раскрытие вуали `rulesReveal` (фокус слева ~10%/50%); входной переносимый титул `rulesCarry` «Правила игры» (pop) → растворяется; `rulesIntro` проявляет левый титул `setRulesTitle(0)` за ~0.7с до контента.
- 3 подслайда `.rslide` (клик листает, `rulesStep` по DOM-порядку). `showRules` → `setRulesTitle(rulesStep)` + `revealRin(rSlides[rulesStep])`. **ЕДИНАЯ логика появления по логическим блокам** (`rinGroups` + `revealRin`, одинаково на всех 3 подслайдах): куски блока — ритм **200мс**, кусок появляется **0.9с** (`GW_APPEAR`), между блоками пауза **500мс** (`GW_PAUSE`).
  1. титул **«ПРАВИЛА ИГРЫ»** · **«Игровой мир: Москва»** (`.gameworld`, data-i=0) — октаэдров нет. Блоки: заголовок [«Игровой мир:», «Москва»] → 3 строки `метка → значение → подпись`: «Действующие игроки:» → «937 тыс. субъектов МСП» → «84% всех предприятий города» · «Занятые (NPC):» → «3,5 млн человек» → «каждый третий работает в МСП» · «Ресурсы (налоги):» → «1,25 трлн рублей» → «каждый четвёртый рубль». Числа `.rnum` — циан-неон-glow.
  2. титул **«УРОВЕНЬ СЛОЖНОСТИ»** · **«Вызовы»** (`.challenges`, data-i=1) — **без h2**. Блок = `.ch-item` [заголовок `.ch-title` + описание `.ch-desc`]. 5 блоков: «Налоговая реформа» · «Высокая ключевая ставка» · «Переход населения к сберегательной модели поведения» · «Изменение потребительских принципов» · «Увеличение доли наличных расчётов и рост теневого сектора» — у каждого своё описание.
  3. титул **«ТРЕНДЫ»** · **«Тренды»** (`.challenges`, data-i=2) — h2 убран. Блок = `.ch-item` [заголовок `.ch-title` + описание `.ch-desc`]. 5 блоков: «Устойчивость бизнес-моделей» · «Цифровизация» · «Качество вместо количества» · «DIGITAL-форматы» · «Региональная экспансия» — у каждого своё описание.
- **Выход:** на последнем подслайде клик → весь текст исчезает → титул **«ПРОВОДНИКИ» по ЦЕНТРУ** (`carryShow(helpersCarry)`, pop) → `rulesCollapse` (фокус «Правительство Москвы») сворачивается в чёрное (титул переживает вуаль) → `avatars`.

## 5. avatars — Проводники

- **Титул «Проводники»**: приходит переносом с правил **по центру**, а на входе слайда (`avEnter` добавляет класс `.avttl`) под чёрной вуалью **перескакивает в ЛЕВЫЙ ВЕРХНИЙ угол** и остаётся весь слайд. Коробка титула = стейдж 3:1 (как `.av-stage`), поэтому угол считается **по картинке, а не по окну** — титул не уходит в чёрную рамку. Гаснет только на выходе.
- **Вход:** раскрытие вуали `avVeil` из фокуса (50%,55%) → через ~1.3с **авто-каскад ВСЕХ 4 аватаров** (`avRunAll`, шаг `AV_STAGGER=1600мс`, **без ожидания клика**). Появление одного: `avSparks`→`play` (искра→рамка/фон→лицо из размытия→**скан контура ×3, без пауз**) → `live` (idle-бумеранг + пульс рамки) → печать подписи.
- **Подпись:** сверху над октаэдром — **подразделение** (мелкий циан), снизу — **ФИО** (боксовая плашка) + **описание**. Порядок печати: подразделение → ФИО → описание.
- Порядок аватаров: МБМ Путилин · ФСК Купринов · **ШТАБ Байков** · **ФПСП Кремнева**. Описания: МБМ «Комплексная поддержка бизнеса от старта до развития» · ФСК «Финансовое обеспечение бизнес-процессов» · ШТАБ «Практическая помощь в проблемных ситуациях» · ФПСП «Поддержка социальных проектов».
- **Выход:** после показа всех — клик (`avShowBtn`) → аватары+подписи **И титул «Проводники» гаснут ВМЕСТЕ** (оба `.6s`), в ЦЕНТРЕ **большая кнопка «НАЧАТЬ БИЗНЕС»** (стиль титульного слайда `.cbtn.cc-big`, размер как центровой титул — `3.6vw`); клик (кнопка/экран) → сворачивание `avVeil` в точку-фокус (50%,55%) → `map`.

## 6. map — Карта (Сцена 3)

Фон — карта Москвы; движок разведки (`runBinoc`/`runMission`/`mapCollapseGo`) общий со слайдом 10. **Ранг героя на карте — 0★ «Новый игрок»** (`RECON.map.heroLevel:0`; подъём уровня на карте убран).

1. **Вход.** С «Проводников» (`avFinish`): особый порядок — карта тёмная (`stage.dark`), но крышный герой ждёт (`mapHeroHold`); титул landing гаснет (~1.1с), через 1300мс landing скрыт → `startMapAuto(0)`. 3 авто-шага (`MAP_STEPS=3`, паузы `MAP_HOLD=[4200,1400]`).
2. **Шаг 0 (тёмный старт).** `playRoofHero` — крышный герой `#roofHero`/`#roofGlow` как аватар: искры → фото из размытия → на **2900мс** контур `roofGlow.glow` + печать имени героя (панель `o-tower`, 0★ «Новый игрок»). Держим `MAP_HOLD[0]`=4200мс.
3. **Шаг 1 (раскрытие).** `stage.reveal` — тёмная вуаль `.darkness` раскрывается; по `transitionend` → шаг 2 (страховка 1800мс). Герой остаётся виден.
4. **Шаг 2 (маршруты+объекты).** `stage.open routes-on` → `startRoutes()`: по маршрутам `routes` бегут **кометы** (`#comets`, скорость `PXMS=0.8` пкс/мс, один проход). Прибытие кометы → узел `active` (+`key` золотом для 4 опор `KEY_NODES`) → `revealObject` (контур `objline.revealed` + октаэдр + печать названия). Объекты без маршрута раскрываются в случайные моменты. **Вывески ЦУБ/ФСК/ФПСП/ШТАБ (`o.sign`) НЕ подсвечиваются** (пропускаются в `revealObject`). Когда все кометы дошли — маршруты гаснут. *(Реплика рассказчика на карте в коде есть, но в потоке не вызывается.)*
5. **Разведка биноклем.** Клик/→ → `startMapRecon()` → `runBinoc(RECON.map)`: `refreshBinocHero(0)` (0★). Осмотр: зум 1.0→1.16 → панорама вправо→влево → на ~2767мс поднять бинокль (`.lenses`, круги `--br:40cqh`). **Тур — 1 стоп: ЦУБ (`o-cowork`)** (`focus:{lift:60,zoomDiv:1.3,clamp:false}`); наведение→приближение с расфокусом→фокус. Панель параметров НЕ печатается (у стопа нет `panel:true`).
6. **Финал → миссия.** `finale:true` → `runMission(RECON.map.mission)`: аватар **Путилина** поверх сфокусированного ЦУБ (искры→рамка→лицо→скан), `.live`; печать ФИО «Путилин А. Р.» → реплика **«Всё начинается с МБМ!»** → расфокус карты → титул **«МИССИЯ 1 / НАЧАТЬ БИЗНЕС»** `pop` в центр; `mapFinaleReady=true`.
7. **Выход.** Клик → `mapCollapseGo()`: `binocVeil` сворачивается в точку ЦУБ (центр) → чёрное; через 1333мс → `mission1` (титул миссии переносится). *(Стрелка во время разведки — `abortRecon` + перепрыгнуть слайд.)*

## 7. mission1 — Миссия 1 (начать бизнес, Путилин)

Фон `cafe-1.png`. **Ранг на входе 0★ «Новый игрок»** → в сюжете level-up 0★→1★.

1. **Вход** (`runMission1`): вуаль `#mission1Veil` закрыта (чёрное), титул «МИССИЯ 1 / НАЧАТЬ БИЗНЕС» виден. Через 1000мс вуаль раскрывается (фокус верх-лево), титул гаснет → по `transitionend` `playCafeHero()`.
2. **Герой на кафе** (`playCafeHero`): искры → `cafeHero.reveal` (фото из размытия) → **2900мс** контур `cafeGlow.glow` (без скана) + обводка вывески `.cafe-sign` + печать имени `CAFE_HERO_NAME=LEVEL_NAMES[0]` «Новый игрок» (0★).
3. **Текст подведа** `m1Text` (нефинансовые услуги: поиск идеи/регистрация/масштабирование, 46 тыс. консультаций…). `cafeIntroDone`.
4. **Клик → `cafeStory`:** текст уходит → аватар **Путилина** (`.live` @1933мс) → ФИО «Путилин А. Р.» (@2500) → реплика 1 «Теперь ты знаешь с чего начать!».
5. **Level-up** `runCafeLevelUp`: **0★→1★**, имя → `LEVEL_NAMES[1]` «Начинающий предприниматель», баннер «УРОВЕНЬ ПОВЫШЕН».
6. Реплика 2 (`typeErase`) «А знаешь ли ты?» → exit-титул **«ВНИМАНИЕ, КВИЗ!»** (`cafeQuizTitle`, z9, `pop`); `cafeStoryDone`.
7. **Выход** (`cafeToQuiz`): через 1600мс АВТО (клик ускоряет) → вуаль в чёрное (титул КВИЗ переживает) → через 2700мс `quiz`.

> Скипы: `skipCafeIntro`, `skipCafeStory`.

## 8. quiz — Миссия 2 · Квиз

**Фон** `.quiz-stage` — **затемнённая карта Москвы** (`Карта Москвы без героя.png` + градиент, темнее слева под вопросом; как слайд 4). Вопрос `.quiz-q` — **формат ТИТУЛА** (Hauss Wide Bold, uppercase, крупный кегль `2.4cqw`, свечение).

1. **Вход** (`runQuiz`): титул «ВНИМАНИЕ, КВИЗ!» (наследован) на чёрном; через 900мс вуаль `quizVeil` раскрывается, титул гаснет.
2. Через ~2400мс — **вопрос 1** (`quizPanel` слева-сверху: счётчик «1 / 5» + вопрос-титул; `quizQR` по центру — один QR `QUIZ_TEST_QR` на все).
3. Клик/→ листает **5 вопросов** (`QUIZ_ITEMS`).
4. После 5-го — клик → `quizEnding`: панель+QR убираются. **Титул «Тем временем…» УБРАН** — переход только вуалями.
5. **Выход** (`quizToNext`, авто через 700мс): `quizVeil` в чёрное → через 2700мс след. слайд + `runLanding('')` — раскрытие вуали landing **без титула**.

## 9. mission1b — Миссия 1, часть 2 (ЕКЦ, Купринов)

Фон `cafe-2.png`. **Ранг 1★ «Начинающий предприниматель»** (без level-up на этом слайде).

1. **Вход:** раскрытие наследуется от квиза через `runLanding`; своя вуаль `#mission1bVeil` держится ОТКРЫТОЙ (только для выхода). Через 1000мс → `playCafe2Hero`.
2. **Герой** (`playCafe2Hero`, копия playCafeHero): искры → фото → 2900мс контур + обводка вывески + имя «Начинающий предприниматель» (1★).
3. **Текст** `m1bText` — «Единый консультационный центр» (ЕКЦ: >400 тематик от 25 ОИВ, «одно окно», ИИ-режим). `cafe2IntroDone`.
4. **Клик → `cafe2Story`:** текст уходит → аватар **Купринова** (`.live` @1933) → ФИО «Купринов А. Э.» (@2500) → **единственная** реплика «А где же наш герой деньги-то возьмёт для развития?». **Level-up НЕ вызывается.**
5. Титул **«ФСК»** (`cafeQuizTitle2`, z9, `pop`); `cafe2StoryDone`. **Авто-перехода нет — нужен клик.**
6. **Выход** (`cafe2ToNext`, по клику): `mission1bVeil` в чёрное (титул «ФСК» переживает) → через 2700мс `binoc` (`binocFromLanding=true`), landing-титул «ФСК» гаснет; карту раскрывает своя `.darkness`.

> Скипы: `skipCafe2Intro`, `skipCafe2Story`.

## 10. binoc — Миссия 2 (найти деньги, ФСК)

Движок разведки общий со слайдом 6. **Ранг 1★ «Начинающий предприниматель»** (`RECON.binoc.heroLevel:1`; функция панели названа `showBinocHeroPanelLvl2`, но фактически ставит 1★).

1. **Вход** (`runBinocMapIntro`, `binocFromLanding`): тёмная карта, крышный герой `playRoofHero` (искры→фото→контур ~2.9с). @5850мс — панель имени (1★ «Начинающий»).
2. @6000мс: `stage.dark→reveal`, `revealObject` всех объектов (под вуалью — проявляются проходом). @+1867мс: `reveal→open`, `runBinoc(RECON.binoc)`.
3. **Разведка:** зум 1.0→1.16 → панорама → @2767мс поднять бинокль. **Тур — 1 стоп: Банк (`o-bank`)**.
4. **Финал** (`runMission`): аватар **Купринова** (искры→скан, `.live`) → ФИО «Купринов А. Э.» → реплика «Нужны деньги на развитие? ФСК поможет — поручительство и льготный кредит!» → расфокус → титул **«МИССИЯ 2 / НАЙТИ ДЕНЬГИ»** `pop`; `mapFinaleReady`.
5. **Выход:** клик → `mapCollapseGo` → `binocVeil` в точку → чёрное → `fsk` (титул продолжается на fsk своим статичным `fskTitle`).

## 11. fsk — ФСК (финансирование, Купринов)

**Ранг на входе 1★** → level-up **1★→2★**.

1. **Вход** (`runFsk`): статичный титул `fskTitle` «МИССИЯ 2 / НАЙТИ ДЕНЬГИ» (наследует из бинокля) на чёрном; через 1000мс вуаль `fskVeil` раскрывается, титул гаснет → `playFskHero`.
2. **Герой** (`playFskHero`): искры → фото → **2900мс** контур `fskGlow.glow` + **обводка вывески ФСК** (`#fskSign`) + имя `FSK_HERO_NAME=LEVEL_NAMES[1]` «Начинающий предприниматель» (1★).
3. **Текст** `fskText` (POD «ФСК — финансирование бизнеса»: поручительство/кредитный брокеридж, 5 гарантийных продуктов · 438 млрд ₽). `fskIntroDone`.
4. **Клик → `fskStory`:** текст уходит → аватар **Купринова** (`.live` @1933) → ФИО «Купринов А. Э.» (@2500) → реплика «Поздравляю! Теперь кафе можно открывать!».
5. **Level-up** `runFskLevelUp`: **1★→2★**, имя → `LEVEL_NAMES[2]` «Действующий предприниматель», баннер.
6. Exit-титул **«ОТКРЫТИЕ КАФЕ»** (`fskOpenTitle`, z9, `pop`); `fskStoryDone`.
7. **Выход** (`fskToNext`, клик): `fskVeil` в чёрное → через 2700мс `cafeopen` (`coFromLanding=true`), landing-титул «ОТКРЫТИЕ КАФЕ» гаснет.

> Скипы: `skipFskIntro`, `skipFskStory`.

## 12. cafeopen — Открытие кафе

Фон-база `cafe-2.png` (ремонт) → проявляется `cafe-3.png` (готово). **Ранг 2★ «Действующий предприниматель».** Авто-показ `runCafeOpen`:

1. **Вход** наследует титул «ОТКРЫТИЕ КАФЕ» (раскрытие landing); своя вуаль `coVeil` открыта. Задержка ~3900мс (ждём landing).
2. **Облако пыли** (`coDust`, 15 клубов `.puff`) + **мультяшные инструменты** (`coTools`, 8 из 5 SVG: молоток/ключ/пила/отвёртка/валик) выскакивают среди пыли.
3. @720мс: под пиком пыли проявляется `cafe-3` (`coBg2`) + вывеска **«Ваш любимый кофе»** (`coCoffee`).
4. @1800мс: пыль+инструменты растворяются. @2100мс: **контур кафе** `coOutline` (прорисовка `coDraw` 2.4с) → @+2400 циан-пульс `glow`.
5. @4800мс: **контур вывески ЦУБ** (`coCub.glow`) + вывеска `coCoffee.lit` (циан-свечение) одновременно.
6. @5400мс: **герой за столиком** `playCoHero` — искры → фото → 2900мс контур + печать имени `LEVEL_NAMES[2]` «Действующий предприниматель» (2★). `coHeroDone`.
7. **Клик → `coStory`:** контур кафе `glow→danger` + вывеска `lit→danger` (**оба → оранжевый** сигнал опасности, синхронно) → пауза 1200мс → аватар **Байкова** (`.live` @1933) → ФИО «Байков С. Н.» (@2500) → реплика «Теперь самое время мне вмешаться?».
8. Клик: exit-титул **«ВНИМАНИЕ, ОПАСНОСТЬ!»** (`coDangerTitle`, z9, `pop`), авто-`coToNext` через 1600мс.
9. **Выход** (`coToNext`): `coVeil` в чёрное (титул переживает) → через 2700мс `danger` (`dangerFromLanding=true`), landing-титул гаснет.

> Скипы: `skipCoIntro` (пыль+герой), `skipCoHero`, `skipCoStory`.

## 13. danger — Опасность (эскаватор, аларм+сирена)

Фон-карта. **Ранг 2★ «Действующий предприниматель»** (`h.stars.filled=2`; комментарий «3★» устарел). Авто-показ `runDanger`:

1. **Вход** (`dangerFromLanding`): `stage.open`, все объекты видны; эскаватор в начале траектории `excPath` (`excavator moving.png`). Задержка ~3900мс.
2. **Герой-наблюдатель** `playDangerHero` (стандартный roof-reveal → 2900мс контур + панель, 2★).
3. @6400мс: **ТРЕВОГА** `.alarm` (красная пульсация по краям экрана) → @+400 **сирена** (`siren.mp3`).
4. @+1600мс: **БИНОКЛЬ** — фон-карта в SVG (`dangerBg`), октаэдры скрыты, герой погашен, линза `dangerBinoc` («глазок», 2 круга), камера `dangerCam` зумит (`DANGER_Z=1.9`) в точку появления эскаватора.
5. @+1300мс: **эскаватор появляется** в зуме — искры (`excSparks`) → фото из размытия (`exc-reveal`) → @+1500 **оранжевый** неон-пульс (`exc-glow`) → `excavatorDrive`.
6. **Едет** по `excPath` 5.2с (камера ведёт следом, пыльный след `dPuff` позади) → у кафе **снос** (`excavatorArrive`): **4 удара ковшом** (ковш вниз+пыль+звёзды, вверх — замах) → граница кафе `o-cowork` **краснеет** (`.danger`) + облако сноса (8 клубов) → @+1400мс паркинг (`excavator_parked.png`).
7. **Байков** (`dgEndStart`, @+2100мс): аватар (`.live` @1933) → ФИО «Байков С. Н.» → реплика «Так, это уже наша работа — бизнес под защитой!» → расфокус бинокля (`dangerBlur`) → @+800мс титул **«МИССИЯ 3 / ЗАЩИТА БИЗНЕСА»** (`pop`).
8. **Выход:** любой клик → `dangerToNext`: `stopAlarm`; landing-титул «МИССИЯ 3 / ЗАЩИТА БИЗНЕСА» → `mission3` (`mission3FromLanding`), титул гаснет.

## 14. mission3 — Миссия 3 (защита бизнеса)

Фон `cafe-3.png` (готовое кафе). **Ранг 2★ «Действующий предприниматель».** 2 кадра по клику.

1. **Вход** (`dangerFromLanding` через landing): титул «МИССИЯ 3 / ЗАЩИТА БИЗНЕСА» → раскрытие; своя вуаль `m3Veil` держится открытой. Через 1500мс → **Кадр 1** (`m3Frame1`).
2. **Кадр 1** — **одновременно**: вывеска **«Ваше любимое кофе»** (`m3Coffee.lit`, циан) + **герой** `m3Hero` (искры→фото) + **эскаватор** `m3Exc` (фото из размытия). @2900мс: эскаватор → **оранжевый** неон (`m3Exc.glow`), контур героя `m3HeroGlow` + имя «Действующий предприниматель» (2★). `m3Frame1Done`.
3. **Клик → Кадр 2** (`m3PlaySpp`): **«Скорая правовая помощь»** (`spp-ambulance.png`) появляется у **Москва-Сити** (дальний `SPP_FRAMES[0]`) — искры→фото → @2900мс **синий** контур + **мигалки** `b0/b1/b2` (`.m3-blink`) → едет 3 кадрами увеличения: средний `SPP_FRAMES[1]` (@+500) → финал `SPP_FRAMES[2]` (@+1300). `m3SppDone`.
4. **Выход:** клик после `m3SppDone` → `fpsp`.

> Скипы: `skipM3Frame1`, `skipM3Spp`.

## 15. fpsp — ФПСП (соц. предпринимательство)

**Статичная подвед-карточка** (`POD.fpsp='pod-fpsp'`), без анимации/аватара.

1. Вход: `applyScene` активирует карточку `data-pod="fpsp"`. Текст `#pod-fpsp`: «ФПСП — социальный бизнес» / «От идеи до масштабирования: обучение, акселерация, краудфандинг, упаковка в социальную франшизу.» / «Университет СП · Акселератор 4.0 · Школа краудфандинга».
2. Выход: клик → `podSlide` (карточка `shtab` наезжает справа-налево, `dir>0`, ~660мс) → `shtab`.

> Аватар Кремневой на этом слайде отсутствует (он только в сцене «Проводники»).

## 16. shtab — Штаб (защита бизнеса)

**Подвед-карточка** (`POD.shtab`) + анимация сноса/защиты `runDemolition`.

1. Вход: карточка `data-pod="shtab"` (только текст) `#pod-shtab`: «Штаб — защита бизнеса» / «Возникла проблема на пути — Штаб защитит: обращения, барьеры, системные вопросы.» / «500+ обращений в год · решение за 15 дней · 120+ объединений». Плюс `runDemolition`.
2. `runDemolition`: (1) **аларм** `.alarm` (красный пульс по краям). (2) @800мс **сирена** (следит за концом трека). (3) @1800мс `startExcavator`: эскаватор въезжает (`EXC_DUR=1400мс`) + пылевой след → снос (`demolish`) → удары ковша → пыль здания.
3. `endDemolition` (по концу сирены/страховке): удары стоп, снос скрыт, **троица появляется** (`showTroika` — въезд снизу за 900мс + бесконечное покачивание); **аларм off + сирена плавно затихает** (`fadeSiren`) — **троица = стоп тревоги**; эскаватор отъезжает.
4. Выход: клик → `podSlide` → `finale`.

> Аватар Байкова на этом слайде отсутствует (он только в сцене «Проводники»).

## 17. finale — Финал

**Статичная подвед-карточка** (`POD.finale`, фон `cafe-3.png`), без анимации/аватара. Последний слайд.

1. Вход: карточка `data-pod="finale"`. Текст `#pod-finale`: «Бизнес открыт и работает» / «Путь пройден вместе: от идеи до своего дела. Город и бизнес — на одной волне.».
2. Конец потока — дальше переходов нет (`cur = SCENES.length-1`).

