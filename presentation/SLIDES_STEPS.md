# Шаги слайдов (источник правды по механикам)

Единый список **текущих шагов на каждом слайде**. Реализация — `presentation/index.html` (сцены в массиве `SCENES`, контроллер `applyScene`).

## Правила ведения

- **При любом изменении механики/шагов слайда — сразу обновляем этот файл** (тот слайд, где менялось).
- **Перед задачей по слайду — читаем его шаги здесь**, чтобы понимать текущую механику.
- Слайды нумеруем 1-based по `SCENES` (совпадает со счётчиком «[N/16]»).
- Детализируем слайд при первом касании; пока не трогали — краткая текущая механика.

## Карта слайдов (SCENES — 16 слайдов)


| #   | id        | Название                                                 |
| --- | --------- | -------------------------------------------------------- |
| 1   | title     | Титул (волны + лого/титул)                               |
| 2   | splash    | Заставка (загрузка → квест-титул)                        |
| 3   | hero      | Знакомство с героем                                      |
| 4   | rules     | Правила игры (3 подслайда)                               |
| 5   | avatars   | Проводники — 4 аватара                                   |
| 6   | map       | Карта: объекты + маршруты → разведка → кафе              |
| 7   | mission1  | МБМ — полный комплекс нефинансовых услуг (Путилин)       |
| 8   | mission1b | МБМ — Единый консультационный центр                      |
| 9   | quiz      | Квиз (ПОСЛЕ консультационного центра)                    |
| 10  | binoc     | Миссия 2 — найти деньги (ФСК, разведка → Банк, Купринов) |
| 11  | fsk       | ФСК — финансирование                                     |
| 12  | cafeopen  | Открытие кафе (ремонт → готово → опасность-эскаватор → Байков) |
| 13  | mission3  | Миссия 3 — защита бизнеса (эскаватор припаркован + скорая) |
| 14  | newcafe   | НОВЫЙ СЛАЙД (копия-база кафе-сцены — правится под задачу) |
| 15  | morebiz   | Больше чем бизнес (герой за стойкой, награда + меню, level-up) |
| 16  | binoc2    | Дубль слайда 10 — герой на крыше + раскрытие карты (без разведки) |

> **Слайд «danger» (Опасность, отдельная карта) УДАЛЁН** — эскаватор-опасность перенесён на слайд 12 (cafeopen).

> **Слайды `fpsp` / `shtab` / `finale` УДАЛЕНЫ полностью** (разметка, `POD`, `podSlide`/`podBreathe`, снос `runDemolition` + троица + узлы SVG). Остались общие: `mk`/`starPts` (искры слайдов 12–13), `#alarm` + `stopAlarm`, `siren.mp3` (сирена скорой на 13). Классы `.podtext`/`.podbig` тоже остались — на них живут тексты кафе-сцен (`#m3Shtab`, `#nc2Text`, `.m1-text`).


> В `SCENES` есть неиспользуемый код `hero2` (копия знакомства с мыслями-облаками), но в потоке слайдов его НЕТ.

### Сквозной ранг героя (`LEVEL_NAMES`, звёзды)

Каждая сцена стартует с ранга, которым закончилась предыдущая. `LEVEL_NAMES` = [0 Новый игрок · 1 Начинающий · 2 Действующий · 3 Продвинутый · 4 Опытный · 5 Московский предприниматель].


| Слайд       | Ранг           | Событие          |
| ----------- | -------------- | ---------------- |
| 6 map       | 0★ Новый игрок | без level-up     |
| 7 mission1  | 0★ → **1★**    | `runCafeLevelUp` |
| 8 mission1b | 1★ → **2★**    | `runCafe2LevelUp` |
| 9 quiz      | 2★ Действующий | без level-up     |
| 10 binoc    | 2★ Действующий | `heroLevel:2`    |
| 11 fsk      | 2★ → **3★**    | `runFskLevelUp`  |
| 12 cafeopen | 3★ Продвинутый | —                |
| 13 mission3 | 3★ → **4★**    | `runLevelUp(m3HeroLU)` |
| 15 morebiz  | 4★ → **5★**    | `runLevelUp(mbHeroLU)` |
| 16 binoc2   | 5★ Московский  | `BINOC2_HERO_LEVEL:5` |


---

## Флаг альтернативного стиля «V5 · срез-смещение»

Пульт: `CFG.style.v5` (по умолчанию `false` — вид не меняется). Переключение без правки кода: **`?style=v5`** в адресе (или `?style=off`). При включении на `<body>` вешается класс `.fx-v5`, и `applyStyleV5()` нарезает помеченные элементы.

| Что | Где | Как меняется |
| --- | --- | --- |
| Лого «Б» | слайд 1, `.nl-logo-svg path` | зарево шире («свет от лого»): 2/10/26/54/90px вместо 1/4/12px |
| «Команда поддержки и развития предпринимательства» | слайд 1, `#nlTxt` | срез-смещение (14 полос, amp 14px) |
| Кнопка «Начать» | слайд 1, `#ccBtn .cbtn-txt` | срез-смещение (10 полос, amp 6px) |
| Слово «КВЕСТ» | слайд 2, `#qtQuest` | срез-смещение; строки «МОСКОВСКИЙ / ПРЕДПРИНИМАТЕЛЬСКИЙ» без изменений |
| Кнопка «Начать бизнес» | слайд 5, `#avCta .cbtn-txt` | срез-смещение (10 полос, amp 6px) |
| «HARD» | слайд 4, подслайд 2, `.rt-hard` | срез-смещение, шрифт LIBRARY 3 AM. Свойства **1-в-1 эталон v5**, отличается **только цвет — красный** `#ff2020` (и полосы, и зарево) |

**Параметры взяты из демо `design-system.html`**, не выдуманы: титул (`#sgDemo`) — `strips 14 · amp 18 · glow 8px · core 2`; кнопка (`.nb-goldframe`) — `strips 10 · amp 6 · glow 6px · core 1`; скорость `0.7×`, яркость `1`. Цвет полос равен цвету зарева (в демо один ползунок красит оба).

**Ядро** (`--sg-core` = радиус, `--sg-corec` = цвет) — раскалённая нить внутри «трубки», первый слой `text-shadow`. В дизайн-системе цвет зашит как `#eaffff` (холодный, почти циан): на синем сливается с заревом, на красном давал бело-синюю сердцевину. Здесь вынесен в токен: для «HARD» — тёплый `#ffdede`. Совсем убрать ядро — `core:0` в `CFG.style.slice.hard`.

**Пульс `hardGlow` при v5 выключен** (`animation:none`): это `filter:drop-shadow` в три слоя ПОВЕРХ полос — свечение выходило вдвое сильнее эталона. Теперь светит только сам срез.

Кнопки и «HARD» при v5 переключаются на **LIBRARY 3 AM**; титулы и так на нём. «HARD» режется не в `applyStyleV5`, а в `fillRulesTitle` — узел `.rt-hard` пересоздаётся при каждом заполнении титула. Правила `.rules-title .rt-hard` специфичнее `.slice-glitch`, поэтому `display`/`color`/`animation` перебиты через `body.fx-v5 …`. Цвет полос и зарева переопределяются 3-м аргументом `sliceEl(el, kind, {color, glowc})` — в пульте это `CFG.style.slice.hard`.

Механика (`buildSlice`, порт из `design-system.html`): текст режется на N полос-копий, каждая обрезана `clip-path` до своей высоты и уезжает по X — на её месте остаётся фон. Метрики элемента не нужны, поэтому полосы строятся один раз при загрузке, даже для скрытых слайдов. `<br>` → `\n` (полосы имеют `white-space:pre`). Параметры полос (кол-во, амплитуда, скорость, зарево, ядро, цвет) — в `CFG.style.slice`.

Разметка: текст титула слайда 1 и слово «КВЕСТ» обёрнуты в `<span>` — эффекты включения (`neonOn`/`neonBoot`) остаются на родителе, срез живёт на span. При выключенном флаге эти span-ы — обычный inline-текст, вид прежний.

---

## Флаг «Лого подразделений» (`CFG.style.logos`)

Независим от `v5`. По умолчанию **включён**; выключить — `?logos=off` в адресе (обратно `?logos=on`). При включении на `<body>` вешается класс `.fx-logos`.

Вместо длинного текстового титула стоит **лого подразделения в левом верхнем углу**:

| Слайд | Узел | Лого | viewBox | размер w×h (cqw) | `--lg-sw` |
| --- | --- | --- | --- | --- | --- |
| 7 mission1 | `#m1Brand` (**новый**, `.dept-bl`) | МБМ | 174×47.6 | 12.8 × 3.51 | 1.76 |
| 8 mission1b | `#m1bBrand` (**новый**, `.dept-bl`) | МБМ | 174×47.6 | 12.8 × 3.51 | 1.76 |
| 9 quiz | `#quizBrand` | МБМ (`MBM.MOS`) | 174×47.6 | 12.8 × 3.51 | 1.76 |
| 11 fsk | `#fskBrand` | Московский гарантийный фонд | 240×48 | 15.0 × 3.00 | 2.08 |
| 13 mission3 | `#m3Brand` (**новый**, `.dept-only`) | Штаб по защите бизнеса | 172.7×49 | 12.6 × 3.58 | 1.78 |
| 14 newcafe | `#nc2Brand` | ФПСП | 58.2×46.8 | 7.5 × 6.02 | 1.01 |
| 15 morebiz | `#mbBrand` | ФПСП | 58.2×46.8 | 7.5 × 6.02 | 1.01 |

**Размещение — левый верхний угол**, отступ `3cqw` по ОБЕИМ осям (`body.fx-logos .fsk-ttl{left:3cqw;top:3cqw;transform:none}`). В процентах поля вышли бы разными: 3% ширины ≈ 58px против 3% высоты ≈ 19px при стейдже 1920×640. `transform:none` сбрасывает `translateY(-50%)`, которым текстовый титул центрировался по `top:27%`.

**Нижний левый угол** — слайды 7, 8, 13, 14 (`.dept-bl` → `top:auto; bottom:3cqw`, тот же отступ 3cqw). Класс задаёт только позицию; видимость у всех по `.show`:
- сл.7/8 (`m1Brand`/`m1bBrand`): лого **появляется перед построчным текстом** — `classList.add('show')` в том же `setTimeout`, что и `revealPod`, до него; при скипе — в паре с `podRinAll`; уходит вместе с текстом (`remove('show')` рядом с каждым `podRinHide`).
- сл.13/14 (`m3Brand`/`nc2Brand`): по `.show` вместе с `m3Shtab` / текстом ФПСП.

Зазоры внизу-слева: сл.7/8 до героя (с 23.4%) — 7.6%; сл.13 ШТАБ (до 15.6%) до героя (с 34.7%) — 19%; сл.14 ФПСП (до 10.5%) до героя `nc-hero` (с 20.1%) и пандуса (с 30.3%) — ≥9.6%. Вывеска кафе на 7/8 пересекается по X, но лежит выше (13–35% высоты).

**Размер — по РАВНОЙ ОПТИЧЕСКОЙ ПЛОЩАДИ**, `S = 45 cqw²`: `h = √(S/ar)`, `w = √(S·ar)`. Выравнивать лого разных пропорций по высоте нельзя: ФСК (5:1) раздавил бы почти квадратный ФПСП (1.24:1), а у ФПСП мелкий текст — всего 9.6% высоты лого, и на общей высоте `h=4cqw` он даёт 7.4px на Full HD (каша). При `S=45` самый мелкий текст — 10.2px на Full HD, 41px на зальном экране 7680. Ниже `S=36` не опускаться: там ФПСП и ФСК подходят к 9px, границе разборчивости для кривых со свечением.

Правые края лого — 10.5–18% ширины; до текстовых колонок (начинаются с 44%) запас ≥26%. Толщина линии `--lg-sw` пересчитана под новые высоты: цель ~0.13cqw на экране у всех четырёх.

**Источник** — ко-бренд-ряд из `assets/img/objects/Титульный Слайд ЛОГО  + титул.svg` (2172×724). Пути вырезаны по колонкам между разделителями, сдвинуты в свой viewBox и сложены в **спрайт `<symbol>`** (`.dept-sprite`, ~70 КБ) — ФПСП используется дважды, дублировать 18 КБ незачем.

**Стиль** — «Логотип · вращение» из `design-system.html`, но без вращения, и **гибридный**: крупная эмблема (высота ≥ 35% лого и ширина/высота < 2.5) — неон-трубка `fill:none` + `stroke`; мелкий текст и широкие текстовые строки — заливка тем же цветом. Сплошной контур сделал бы буквы высотой ~5px («МАЛЫЙ БИЗНЕС МОСКВЫ», три строки ФПСП) нечитаемыми. Зарево `drop-shadow` 1/4/12px — 1-в-1 как у лого «Б» (`.nl-logo-svg path`).

> **ФСК — исключение: эмблема целиком заливкой.** Автоклассификатор увёл её три наклонные полосы (2 шеврона 45×45 и полоса 15×45) в контур, и они читались полыми рядом с тремя залитыми квадратами 15×15. Все шесть фигур теперь сплошные, одним цветом. Контурных путей в `lg-fsk` не осталось.

Стили внутри `<symbol>` заданы **inline через переменные** (`--lg-c` цвет, `--lg-sw` толщина): CSS-селекторы документа не достают внутрь shadow-дерева `<use>`, наследуются только переменные. `--lg-sw` в единицах viewBox, поэтому у каждого лого свой (1.2 / 1.45 / 1.23 / 0.78) — иначе при разном масштабе линия вышла бы разной толщины; цель ~0.2cqw у всех.

Переключение — чистый CSS: `.ttl-txt` прячется, `.dept-logo` показывается **внутри того же `.fsk-ttl`**, поэтому логика показа сцен (`.show`) не изменилась. Исключение — слайд 13: там титула не было, добавлен отдельный `#m3Brand`, он ходит вместе с `m3Shtab` (`m3ShowShtab` / `m3DoLevelUp` / `resetMission3`).

---

## 1. title — Титул

- Фон: `#ccanvas` — волновое поле (canvas) + `cover.png` (частицы) + оверлей `ccTitleArt` (лого-строка + титул `title-lockup.svg`, только на титуле). SVG — обновлённая версия «Титульный Слайд ЛОГО + титул»: доп. глифы-логотипы + нижняя строка «КОМАНДА ПОДДЕРЖКИ И РАЗВИТИЯ ПРЕДПРИНИМАТЕЛЬСТВА» шрифтом **LIBRARY 3 AM** (неон) — **текст переведён в кривые (`<path>`)** через opentype.js (`<img>`-SVG в Chrome не рендерит `@font-face` надёжно), поэтому шрифт не нужен, рендерится везде одинаково.
**Шаги по клику (`ccStage` 0 → 1 → 2).** Шаг 1 существует только при `CFG.music.titleIntro:true`; при `false` первый клик сразу даёт шаг 2, и титульной мелодии нет вовсе.

- `ccStage 0` — волны, тишина. **1-й клик** (`ccMusicOn`): включается **титульная мелодия** `bgPlay('title')` — фрагмент **0–20с** файла `bg-title.mp3` (границы в `BG_CLIP.title`), зациклен кроссфейдом. Визуально не меняется ничего, кроме текста подсказки («нажмите ещё раз») — иначе клик читается как зависание.
- `ccStage 1` — **2-й клик** (`ccClose`): **вуаль закрытия** `ccVeil` в чёрное (0.7с) → на чёрном **большая кнопка «Начать»** (`.cbtn.cc-big`, Hauss Wide, ~8cqh). Тут же `bgPlay('load')` — кроссфейд с титульной мелодии на «Загрузку».
- `ccStage 2` — клик (кнопка/любой) → переход на `splash`. Там `applyScene` зовёт `bgPlay('load')` — тот же трек, значит **no-op**: музыка не перезапускается, переход титул→заставка не слышен.
- `←` откатывает по одному шагу: с 2 → волны + титульная мелодия (или тишина при `titleIntro:false`), с 1 → волны молча. `applyScene` в этих ветках не вызывается, поэтому трек и текст подсказки переключаем вручную. Возврат на титул из заставки идёт через `applyScene` → `ccStage=0` и тишина.

> **Зачем отдельный клик под музыку.** Браузер не пускает звук до жеста пользователя. Заведи мелодию сразу при открытии — автоплей её заблокирует, а первый же клик уводил бы на «Загрузку», и титульная мелодия не прозвучала бы ни разу. Отдельный шаг это решает: клик даёт жест, музыка играет, следующий клик двигает сцену. Флаг `CFG.music.titleIntro:false` возвращает старое поведение.

## 2. splash — Заставка

**Механика:** экран загрузки → квест-титул увеличением → уход с переносом титула через вуаль («титул переживает вуаль»).

**Загрузка** (`runLoading`, проигрывается **ВСЕГДА**, в т.ч. при возврате; **пропуск любым кликом/→**):
Элементы появляются **по-очереди с паузами** (не одновременно):

1. **Сразу** — полоса `loadFill` плавно **0→100% за ~4.5с** (`LOAD_DUR=4533`) + процент `loadPct`.
2. **Пауза ~700мс → лог** — 4 строки по-очереди (слот в оставшемся времени): текст + мигающие «…» → через ~0.7 слота **галка ✓ + стоп** (done). Строки: «Ищу идею» · «Нахожу консультанта» · «Определяю локации» · «Подбираю субсидии».
3. **Пауза ~1.55с → подпись** `loadCap` появляется и циклится каждые 1.5с: «Гружу» → «Гружу-гружу» → «Гружу, ещё чуть-чуть».
4. Бар 100% → `finishLoading`. *(Клик/→ до конца загрузки → `finishLoading` пропускает сразу.)*

**Титул** (`finishLoading`):
5. `splash.loaded` — плавно прячутся бар/процент/подпись/лог.
6. Через **450мс** — квест-титул `questTitle` **зажигается «ЭФФЕКТОМ 2»** (`neonOn .9s` → `neonIdle 3.6s infinite`, точь-в-точь как в `design-system.html`): резкая вспышка → провал → спокойное свечение → вечное idle-мерцание. Мерцание висит на обёртке `.qt-line`, а не на лого — иначе `brightness` стёр бы его `drop-shadow`.
   Титул — **HTML-локап** по макету `ТИТУЛ МПК после заставки.svg`: `.qt-logo` (инлайн-SVG лого «Б», неон-трубка) + `.qt-txt` (три строки «МОСКОВСКИЙ / ПРЕДПРИНИМАТЕЛЬСКИЙ / КВЕСТ», шрифт **LIBRARY 3 AM** = `--n-lib`). Пропорции макета переведены в `em` от кегля 60: лого `2.52×3.14em`, зазор `.64em`, интерлиньяж `1.233`. Кегль `min(3.64vw,70px)`, блок по центру.
   **Лого ВРАЩАЕТСЯ** — та же схема, что на слайде 1: `perspective:700px` на `.qt-logo-box` → `nlSpin 7s linear infinite` на `.qt-logo-spin` (`transform-style:preserve-3d`) → `svg.qt-logo` внутри. Бокс держит ширину, поэтому текст не «дышит», когда лого проходит ребро.
   **Свечение — свойства дизайн-системы, не самодельные:** текст = `.glow-title.n-blue` (белая заливка + тёмная база `0 2px 8px rgba(0,0,0,.95)` + УЗКИЙ циан `1/5/14px` — без широкого зарева и плашки-ореола), лого = неон-трубка `.logo-box` (ядро `#dffaff` + зарево `1.4/5.6/17px`, пересчитано под свой viewBox). *(В исходном SVG была опечатка «ПРЕДПРИНИМТЕЛЬСКИЙ» — исправлена.)*
7. *(Текстовый титул «НА ОДНОЙ ВОЛНЕ 2026» и обе печати «Новый / импульс» удалены — вместе с CSS `.quest-stamp`/`stampSlam`/`stampFrameGlow`/`stampTextGlow`.)*

**Выход** (клик/→ после титула, `splashLeave`):
8. Титул пиним по экранным координатам (fixed) → **переносим тот же узел** `questTitle` в оверлей `questCarry` (над сценами) — без прыжка. Анимацию подменяем на `neonIdle` (без `neonOn`), иначе перенос узла зажёг бы титул заново; мерцание при этом сохраняется. Кегль задан в `vw` (не в `%`), поэтому при пине не меняется.
9. Вуаль `splashVeil` сворачивается фокусом **в центр (50%,50%)**: `--r` 175%→0% → чёрный → переход на `hero`.
10. На `hero` при раскрытии титул `questCarry` **растворяется** (fade ~1.9с). Возврат — узел домой (`questHome`), пин снят.

## 3. hero — Знакомство с героем

> Внутренняя подготовка (не шаг): `buildHeroQuestions` строит DOM мыслей + `resetHeroText` чистит текст.

1. **Вуаль раскрытия** из фокуса **(46%, 38%)**: `--r` 6% → **108% за 2с** (ровно до края экрана, без захода за экран).
2. **Контур героя** `hero-outline` — show + glow **сразу** (без прорисовки сверху-вниз).
3. **На 2000мс** (конец раскрытия): печать имени **«Новый игрок»** в чипе `.herobox`, **45мс/символ**, без звёзд (`typeChipName`).
4. По завершении имени — выноска `heroLead` (линия + точка к герою).
5. Через **400мс** — мысли героя **печатаются по одной** (`typeThought`, белый Hauss, 45мс/символ; следующая через 400мс после конца предыдущей). Порядок **последовательный**: сначала **левые сверху вниз**, затем **правые сверху вниз** (без рандома). **8 вопросов (4 слева + 4 справа)** разложены **ПО ЭЛЛИПСУ** вокруг героя (`assets/img/objects/Мысли героя.svg`: `cx1048 cy362 rx327.5 ry353` в viewBox 2172×724). `HERO_QUESTIONS[{t,side,edge,y}]`: **`side:'l'`** — слева, якорь по **правому краю** (`right:edge%`, выключка вправо `.aright`) к левой дуге, **СТИЛЬ 1** (золото `thinkGlow`); **`side:'r'`** — справа, якорь по **левому краю** (`left:edge%`, выключка влево) к правой дуге, **СТИЛЬ 2** (циан-неон `thinkGlowNeon`). `edge` = x дуги на середине блока: `cx ± rx·√(1 − ((ym − cy)/ry)²)`, где `ym = y + 3.5%` (2 строки) или `y + 1.75%` (1 строка). Левые (сверху вниз): «Как найти идею для открытия бизнеса?» · «Как зарегистрировать бизнес?» · «Как подобрать локацию?» · «Где взять деньги?». Правые: «Как повысить эффективность бизнеса?» · «Как защитить свой бизнес?» · «Как оценить пользу моего бизнеса?» · «Как сделать бизнес более технологичным?».
6. **Выход** (клик/→) `heroLeave`: стоп печати → `collapsing` (скрыть имя/выноску/мысли) → вуаль сворачивается в фокус **Москва-Сити (92%, 24%)**, `--r`→0 за **3.4с** → переход на `rules`. Клик во время сворачивания → `heroFinish` сразу. **Титул «Правила игры» здесь НЕ показывается** — он появляется как вход на слайд 4.

## 4. rules — Правила игры

- **У КАЖДОГО подслайда СВОЙ левый титул** `#rulesTitle` (`.rules-title`, uppercase, слева; **фикс-позиция на уровне заголовка «Игровой мир» — `top:27%` на всех подслайдах**; на входе титул **ПРОЯВЛЯЕТСЯ СРАЗУ В ДОКЕ** (fade `opacity .5s`, `enterRulesTitle` — без центра/переезда); `white-space:normal` + `max-width:36cqw` — длинный переносится в 2 строки): `RULES_TITLES` = [0 «Правила игры» · 1 «Уровень сложности» · 2 «Тренды»]. Смена = `setRulesTitle(step)` — **скрыть мгновенно → сменить текст → плавно ПРОЯВИТЬ** (fade `.6s`); на 2-м/3-м подслайде титул проявляется при переходе.
- **Вход:** чёрное → раскрытие вуали `rulesReveal` (фокус слева ~10%/50%); входной переносимый титул `rulesCarry` «Правила игры» (pop) → растворяется; `rulesIntro` → `enterRulesTitle` проявляет левый титул СРАЗУ в доке, через `TITLE_APPEAR=800мс` — контент.
- 3 подслайда `.rslide` (клик листает, `rulesStep` по DOM-порядку). `showRules` → `setRulesTitle(rulesStep)` + `revealRin(rSlides[rulesStep])`. **ЕДИНАЯ логика появления по логическим блокам** (`rinGroups` + `revealRin`, одинаково на всех 3 подслайдах): куски блока — ритм **200мс**, кусок появляется **0.9с** (`GW_APPEAR`), между блоками пауза **500мс** (`GW_PAUSE`).
  1. титул **«ПРАВИЛА ИГРЫ»** · **«Игровой мир: Москва»** (`.gameworld`, data-i=0) — октаэдров нет. Блоки: заголовок [«Игровой мир:», «Москва»] → 3 строки `метка → значение → подпись`: «Игроки:» → «937 тыс. субъектов МСП» → «84% всех предприятий города» · «Союзники: / (занятые)» → «3,5 млн человек» → «каждый третий работает в МСП» · «Ресурсы: / (налоги)» → «1,25 трлн рублей» → «каждый четвёртый рубль». Двухстрочные метки — `.gw-label.gw-2line` (плотный интерлиньяж) + `<br>` + уточнение в `.gw-sub`. Числа `.rnum` — циан-неон-glow.
  2. титул **«УРОВЕНЬ СЛОЖНОСТИ»** · **«Вызовы»** (`.challenges`, data-i=1) — **без h2**. Блок = `.ch-item` [заголовок `.ch-title` + описание `.ch-desc`]. 5 блоков: «Налоговая реформа» · «Высокая ключевая ставка» · «Переход населения к сберегательной модели поведения» · «Изменение потребительских принципов» · «Увеличение доли наличных расчётов и рост теневого сектора». Описания — **короткие, в одну фразу** (без вводных оборотов): «Изменение НДС и ставок страховых взносов» · «Дорогие деньги ограничили возможности развития МСП» · «Оптимизация расходов и снижение покупательской активности» · «Устойчивое перераспределение товарных потоков в пользу маркетплейсов» · «Практика „скидки без чека“ и „скидка за наличку“, фиктивные возвраты». Под титулом — красное **«HARD»** (`.rt-hard`, `hardGlow`), проявляется ПОСЛЕ последнего блока (`RULES_HARD_STEP=1`).
  3. титул **«ТРЕНДЫ»** · **«Тренды»** (`.challenges`, data-i=2) — h2 убран. Блок = `.ch-item` [заголовок `.ch-title` + описание `.ch-desc`]. **4 блока** (5-й, «Региональная экспансия», удалён): «Устойчивая бизнес-модель» → «Оптимизация производственных процессов, в том числе за счёт интеграции ИИ» · «Цифровизация» → «CRM, аналитика и автоматизация — обязательное условие для роста и развития» · «Качество вместо количества» → «Повышение качества товаров/работ или услуг, повышение клиентоориентированности» · «DIGITAL-форматы» → «Лёгкость масштабирования и экономия на аренде, персонале и логистике».
- **Выход:** на последнем подслайде клик → весь текст исчезает → титул **«ПРОВОДНИКИ» по ЦЕНТРУ** (`carryShow(helpersCarry)`, pop) → `rulesCollapse` (фокус «Правительство Москвы») сворачивается в чёрное (титул переживает вуаль) → `avatars`.

## 5. avatars — Проводники

- **Титул «Проводники»** (шаблон движения, как слайд 4): приходит переносом с правил **по центру**; на входе слайда держится в центре (класс `.avttl.tcenter`), затем через `TITLE_HOLD5=1000мс` **ПЛАВНО ЕДЕТ в ЛЕВЫЙ ВЕРХНИЙ угол** (снимается `.tcenter`, CSS-переход `top/left/transform/font-size` **.9с** → док `top:4%/left:3%`). Коробка титула = стейдж 3:1 (`.avttl.show`), поэтому угол считается **по картинке, а не по окну** — не уходит в чёрную рамку. Остаётся весь слайд, гаснет на выходе. Аватары стартуют **после переноса титула** (`TITLE_HOLD5+TITLE_MOVE5 ≈ 1900мс`).
- **Вход:** раскрытие вуали `avVeil` из фокуса (50%,55%) → через ~1.3с **авто-каскад ВСЕХ 4 аватаров** (`avRunAll`, **без ожидания клика**). **Следующий аватар — ПОСЛЕ проявления всех блоков предыдущего** (`AV_STAGGER = AV_DESC_AT + fade + пауза ≈ 3950мс`; старты 0·3950·7900·11850). Появление одного: `avSparks`→`play` (искра→рамка/фон→лицо из размытия→**скан контура ×3, без пауз**) → `live` (idle-бумеранг + пульс рамки) → подписи ПРОЯВЛЯЮТСЯ.
- **Подпись (проявляется, НЕ печатается):** сверху над октаэдром — **подразделение** (белый, `.ava-org`, подсветка **стиль 2** `thinkGlowNeon`), снизу — **ФИО** (боксовая плашка) + **описание**. Блоки fade+подъём `.55с`, **между блоками 200мс**: подразделение `AV_ORG_AT=1900` (с лицом) → ФИО `2100` → описание `2300`. `AV_STAGGER` авто = `AV_DESC_AT+550+350 ≈ 3200мс`.
- Порядок аватаров: МБМ Путилин · ФСК Купринов · **ШТАБ Байков** · **ФПСП Кремнева**. Описания: МБМ «Комплексная поддержка бизнеса от старта до развития» · ФСК «Финансовое обеспечение бизнес-процессов» · ШТАБ «Правовая помощь в сложных ситуациях» · ФПСП «Поддержка социальных проектов».
- **Выход:** после показа всех — клик (`avShowBtn`) → аватары+подписи **И титул «Проводники» гаснут ВМЕСТЕ** (оба `.6s`), в ЦЕНТРЕ **большая кнопка «НАЧАТЬ БИЗНЕС»** (стиль титульного слайда `.cbtn.cc-big`, размер как центровой титул — `3.6vw`); клик (кнопка/экран) → сворачивание `avVeil` в точку-фокус (50%,55%) → `map`.

## 6. map — Карта (Сцена 3)

Фон — карта Москвы; движок разведки (`runBinoc`/`runMission`/`mapCollapseGo`) общий со слайдом 10. **Ранг героя на карте — 0★ «Новый игрок»** (`RECON.map.heroLevel:0`; подъём уровня на карте убран).

1. **Вход.** С «Проводников» (`avFinish`): особый порядок — карта тёмная (`stage.dark`), но крышный герой ждёт (`mapHeroHold`); титул landing гаснет (~1.1с), через 1300мс landing скрыт → `startMapAuto(0)`. 3 авто-шага (`MAP_STEPS=3`, паузы `MAP_HOLD=[4200,3400]`).
2. **Шаг 0 (тёмный старт).** `playRoofHero` — крышный герой `#roofHero`/`#roofGlow` как аватар: искры → фото из размытия → на **2900мс** контур `roofGlow.glow` + печать имени героя (панель `o-tower`, 0★ «Новый игрок»). Держим `MAP_HOLD[0]`=4200мс.
3. **Шаг 1 (раскрытие).** `stage.reveal` — тёмная вуаль `.darkness` раскрывается за **3.4с** (= шаблон `.hveil`), цель **`--r:104%`** (впритык к экрану, 100%=дальний угол — без невидимого хвоста до 135%). По `transitionend` (≈ момент полного раскрытия) → шаг 2, кометы стартуют сразу (страховка `MAP_HOLD[1]+400`=3800мс). Герой остаётся виден.
4. **Шаг 2 (маршруты+объекты).** `stage.open routes-on` → `startRoutes()`: по маршрутам `routes` бегут **кометы** (`#comets`, скорость `PXMS=0.8` пкс/мс, один проход). Прибытие кометы → узел `active` (+`key` золотом для 4 опор `KEY_NODES`) → `revealObject` (контур `objline.revealed` + октаэдр + печать названия). Объекты без маршрута раскрываются в случайные моменты. **Вывески ЦУБ/ФСК/ФПСП/ШТАБ (`o.sign`) НЕ подсвечиваются** (пропускаются в `revealObject`). Когда все кометы дошли — маршруты гаснут. *(Реплика рассказчика на карте в коде есть, но в потоке не вызывается.)*
5. **Разведка биноклем.** Клик/→ → `startMapRecon()` → `runBinoc(RECON.map)`: `refreshBinocHero(0)` (0★). Осмотр: зум 1.0→1.16 → панорама вправо→влево → на ~2767мс поднять бинокль (`.lenses`, круги `--br:40cqh`). **Тур — 1 стоп: ЦУБ (`o-cowork`)** (`focus:{lift:60,zoomDiv:1.3,clamp:false}`); наведение→приближение с расфокусом→фокус. Панель параметров НЕ печатается (у стопа нет `panel:true`).
6. **Финал → миссия.** `finale:true` → `runMission(RECON.map.mission)`: аватар **Путилина** поверх сфокусированного ЦУБ (искры→рамка→лицо→скан), `.live`; печать ФИО «Путилин А. Р.» → реплика **«Всё начинается с МБМ!»** → расфокус карты → титул **«МИССИЯ 1 / НАЧАТЬ БИЗНЕС»** `pop` в центр; `mapFinaleReady=true`.
7. **Выход.** Клик → `mapCollapseGo()`: `binocVeil` сворачивается в точку ЦУБ (центр) → чёрное; через 1333мс → `mission1` (титул миссии переносится). *(Стрелка во время разведки — `abortRecon` + перепрыгнуть слайд.)*

> **ПОРЯДОК ИЗМЕНЁН:** квиз переехал с 8 на **9** (идёт ПОСЛЕ консультационного центра), `mission1b` (ЕКЦ) — с 9 на **8**. Поток: `mission1 → mission1b → quiz → binoc`. Титул **«ВНИМАНИЕ, КВИЗ!»** переехал с выхода слайда 7 на выход слайда 8 (`cafeQuizTitle2`).

### Общий стиль слайдов 7–9 (как слайд 11)

- **ПЕРСИСТЕНТНЫЙ титул МБМ СЛЕВА** — «Малый / бизнес / Москвы» (`.fsk-ttl`, 3 строки, uppercase, весь неон; координаты титула слайда 4: `left:6cqw; top:27%`). Id: `m1Brand` / `m1bBrand` / `quizBrand`.
- **Справа** — `.fsk-slides` (top-align по верху титула): **заголовок** `.fsk-h` + буллиты `.ch-item` (иконка + текст). Иконки — свои неон-SVG (`.gw-ico`).
- **Появление блоков ПО ОЧЕРЕДИ с паузами** — общий помощник `revealPod(el, timers)` + `podGroups` / `podRinAll` / `podRinHide` (через `.rin` и `CFG.reveal`).
- **Акценты** — `.fsk-ac` (неон-слово) и `.rnum` (неон-число).
- **ЗАТЕМНЕНИЕ `.m1-dark` — НЕ сразу, а ПРОЯВЛЯЕТСЯ вместе с лого+текстом (как слайд 13).** `.m1-dark` теперь `opacity:0 → .show` (fade `.8s`), не статичное. Id: `m1Dark` (сл.7) / `m1bDark` (сл.8) / `fskDark` (сл.11). `.show` вешается/снимается **в паре с брендом**: показ — рядом с `m1Brand`/`m1bBrand`/`fskBrand` `.add('show')` (перед построчным текстом / в `fskShowSub`); скрытие — рядом с `.remove('show')` (в `cafeStory`/`cafe2Story`/`fskFadeOut`/`fskHideSubs` и в reset'ах). Герой появляется на СВЕТЛОМ фоне, затем затемнение наплывает под текст.

## 7. mission1 — МБМ: полный комплекс нефинансовых услуг (Путилин)

Фон `cafe-1.png`. **Ранг на входе 0★ «Новый игрок»** → в сюжете level-up 0★→1★.

1. **Вход** (`runMission1`): вуаль `#mission1Veil` закрыта (чёрное), титул «МИССИЯ 1 / НАЧАТЬ БИЗНЕС» виден. Через 1000мс вуаль раскрывается (фокус верх-лево), титул гаснет → по `transitionend` `playCafeHero()`.
2. **Герой на кафе** (`playCafeHero`): искры → `cafeHero.reveal` (фото из размытия) → **2900мс** контур `cafeGlow.glow` + обводка вывески `.cafe-sign` + печать имени `LEVEL_NAMES[0]` «Новый игрок» (0★).
3. **Титул МБМ** (`m1Brand`) + **текст** `m1Text` — заголовок «Полный комплекс нефинансовых услуг для бизнеса» + 5 буллитов (построчно, `revealPod`): Поиск **идеи** и планирование · **Регистрация** бизнеса · Поиск **помещения** · Меры **поддержки** и госзакупки · Рост **эффективности** и продвижение. `cafeIntroDone`.
4. **Клик → `cafeStory`:** текст + титул МБМ уходят → **пауза 1000мс** → **level-up `runCafeLevelUp` 0★→1★**, имя → `LEVEL_NAMES[1]` «Начинающий предприниматель», баннер. **Аватара и реплик НЕТ.**
5. **Выход** (`cafeToQuiz`): через 1600мс АВТО (клик ускоряет) → вуаль в чёрное → `mission1b` под чёрным + `runLanding('')` раскрывает следующую сцену. **Титула НЕТ** (КВИЗ переехал на слайд 8).

> Скипы: `skipCafeIntro`, `skipCafeStory`.

## 8. mission1b — МБМ: Единый консультационный центр

Фон `cafe-2.png`. **Ранг на входе 1★ «Начинающий»** → level-up **1★→2★**.

1. **Вход:** раскрытие наследуется от слайда 7 через `runLanding`; своя вуаль `#mission1bVeil` держится ОТКРЫТОЙ (только для выхода). Через 1000мс → `playCafe2Hero`.
2. **Герой** (`playCafe2Hero`): искры → фото → 2900мс контур + обводка вывески + имя «Начинающий предприниматель» (1★).
3. **Титул МБМ** (`m1bBrand`) + **текст** `m1bText` — заголовок «Единый консультационный центр» + 4 буллита (построчно): Консультации в режиме **«одного окна»** · **>400** тематик поддержки · **>25** ОИВ Москвы · ИИ-агент **24/7** в помощь консультантам. `cafe2IntroDone`.
4. **Клик → `cafe2Story`:** текст + титул МБМ уходят → **пауза 1000мс** → **level-up `runCafe2LevelUp` 1★→2★**, имя → `LEVEL_NAMES[2]` «Действующий предприниматель», баннер → @4200мс exit-титул **«ВНИМАНИЕ, КВИЗ!»** (`cafeQuizTitle2`, z9, `pop`); `cafe2StoryDone`.
5. **Выход** (`cafe2ToNext`, по клику): `mission1bVeil` в чёрное (титул КВИЗ z9 переживает) → `quiz` (раскроется своей вуалью `runQuiz`).

> Скипы: `skipCafe2Intro`, `skipCafe2Story` (снап level-up через `cafe2LevelFinal` + титул КВИЗ).

## 9. quiz — Квиз (после консультационного центра)

**Фон** `.quiz-stage` — затемнённая карта Москвы + градиент **как слайд 4/7/8 (тёмное СПРАВА** под вопросом). **Ранг 2★** (панели героя нет). Стиль текста — **как слайд 11**.

Раскладка: **титул МБМ слева** (`quizBrand`, верх) · **QR по центру ЛЕВОЙ половины** (`.quiz-qr`, `left:23%; top:60%; 38cqh; translate(-50%,-50%)` — ниже центра, из-под 3-строчного титула) · **счётчик + вопрос + варианты справа** (`#quizPanel` = `.fsk-slides`).

- **Счётчик «1 / 5»** — АКЦЕНТ: белое ядро + циан-неон пульс (`text-shadow:var(--sh-base)` + `thinkGlowNeon`), `1.2cqw`, letter-spacing `.28em`.
- **Вопрос** — ЗАГОЛОВОК слайда (`.fsk-h`), `2.6cqw`.
- **3 варианта ответа** (`.quiz-answers`, под вопросом, выключка по его левому краю): октаэдр-буллит **А/Б/В** (`quizMark(i)`: гекс `polygon` + `<text>` В ОДНОМ `.q-hex` SVG → вращаются вместе `hexspin` rotateY) + текст `.quiz-ans-txt` (`--sz-body`). Появляются **по очереди** после вопроса.

1. **Вход** (`runQuiz`): титул «ВНИМАНИЕ, КВИЗ!» (наследован со слайда 8) на чёрном; через 900мс вуаль `quizVeil` раскрывается, титул гаснет.
2. Через ~2400мс — **вопрос 1** (`showQuizItem`: прежний уходит fade 300мс → счётчик → вопрос → 3 варианта по очереди, ритм `CFG.reveal`).
3. Клик/→ листает **5 вопросов** (`QUIZ_ITEMS = {q, a:[3], qr}`, один QR `QUIZ_TEST_QR` на все). Варианты из «Квиз про МБМ.docx» где совпал вопрос; для «Сколько консультаций в год» и «Где ни разу не бывал флаг МБМ» ответов в файле нет → **ЗАГЛУШКИ** «Вариант ответа 1/2/3» (заменить). Вопросы: «Сколько лет ГБУ „МБМ“?» (7/9/14) · «Сколько консультаций в год…» (заглушки) · «Сколько мероприятий…» (Более 100/250/500) · «Какое имя самое популярное…» (Елена/Галина/Лидия) · «Где ни разу не бывал флаг МБМ?» (заглушки).
4. После 5-го — клик → `quizEnding`: вопрос+варианты+титул+QR убираются. Титул «Тем временем…» УБРАН.
5. **Выход** (`quizToNext`, авто через 700мс): `quizVeil` в чёрное → landing (без титула) + `binocFromLanding=true` → `binoc`; карту раскроет своя `.darkness`.

> Скипы: `skipQuizEnding`.

## 10. binoc — Миссия 2 (найти деньги, ФСК)

Движок разведки общий со слайдом 6. **Ранг 2★ «Действующий предприниматель»** (`RECON.binoc.heroLevel:2`; `showBinocHeroPanel` берёт ранг из `RECON.binoc.heroLevel`). **Начало по таймингам = слайд 6** (герой `playRoofHero`, имя @2900, карта @4200); отличия от слайда 6 — **нет маршрутов/комет** (все объекты уже виделись на карте — показываются разом) и ранг выше.

1. **Вход** (`runBinocMapIntro`, `binocFromLanding`): тёмная карта, крышный герой `playRoofHero` (искры→фото→контур @2900). @2900мс — панель имени (2★ «Действующий»).
2. @4200мс (как `MAP_HOLD[0]` на слайде 6): `stage.dark→reveal`, `revealObject` всех объектов **разом** (под вуалью — раскрываются проходом за **3.4с** = шаблон `.hveil`, без маршрутов). @+3400мс (конец раскрытия): `reveal→open`, `runBinoc(RECON.binoc)`.
3. **Разведка:** зум 1.0→1.16 → панорама → @2767мс поднять бинокль. **Тур — 1 стоп: Банк (`o-bank`)**, `focus:{lift:60, zoomDiv:1, cover:0.7, clamp:true}` — прицел поднят, Банк по центру круга.
4. **Финал** (`runMission`): аватар **Купринова** (искры→скан, `.live`) → ФИО «Купринов А. Э.» → реплика в **2 строки** («Нужны деньги на развитие?» / «ФСК поможет с поручительством и льготным кредитом») → расфокус → титул **«МИССИЯ 2 / НАЙТИ ДЕНЬГИ»** `pop`; `mapFinaleReady`.
   *Перенос строки:* `\n` в `say` + `.hero-say .say-txt{white-space:pre-line}` (`typeSay` пишет `textContent`, `<br>` не сработал бы). Вторая строка шире октаэдра, поэтому пузырю точечно задан `#missionSay{width:max-content}` — иначе `.mission-say{width:100%}` разбил бы её на 3 строки.
5. **Выход:** клик → `mapCollapseGo` → `binocVeil` в точку → чёрное → `fsk` (титул продолжается на fsk своим статичным `fskTitle`).

## 11. fsk — ФСК (финансирование, Купринов)

**Ранг на входе 2★** → level-up **2★→3★**. **3 подслайда ФСК** (`#fskSlides` → 3 `.fsk-sub`, счётчик `fskStep` 0/1/2): герой Купринов появляется 1 раз (подслайд 1) и **остаётся**, между подслайдами **меняется только заголовок+блоки**. Level-up — только **после ухода текста 3-го подслайда**. Контент взят из «Концепция_ФСК+.pptx» (3 слайда), стиль — **как слайд 4**:
> - **ПЕРСИСТЕНТНЫЙ титул СЛЕВА** `#fskBrand` «Фонд / содействия / кредитованию» (**3 строки** через `<br>`; `.fsk-ttl` = координаты титула слайда 4 `.rules-title`: `left:6cqw; top:27%`, **uppercase**, **весь неон** `thinkGlowNeon`) — один на все 3 подслайда, появляется 1 раз и остаётся.
> - **ЗАГОЛОВОК** подслайда `.fsk-h` (`2.15cqw`, крупнее основного текста) — тема подслайда, свой на каждом. Правый блок `.fsk-slides` **top-align по верху титула** (`top:calc(27% − 5cqw)`, `justify-content:flex-start`) — текст растёт ВНИЗ, не поднимается выше титула независимо от объёма.
> - Подслайды 1-2 — буллиты `.ch-item` (иконка+заголовок, описание ПОД ним, как «Вызовы»); подслайд 3 — цифры `.gw-list` (метка → `.rnum` циан-число → подпись) + хайлайты `.fsk-hi` строками друг под другом.
> - **Появление блоков ПО ОЧЕРЕДИ с паузами** (как слайд 4): `fskRevealSub` через `.rin` + `CFG.reveal` (blockStep/blockShow/blockGap), таймеры `fskRinT`.
> - Иконки — свои неон-SVG (`.gw-ico`, стиль слайда 4).

1. **Вход** (`runFsk`): статичный титул `fskTitle` «МИССИЯ 2 / НАЙТИ ДЕНЬГИ» (наследует из бинокля) на чёрном; через 1000мс вуаль `fskVeil` раскрывается, титул гаснет → `playFskHero`.
2. **Герой** (`playFskHero`): искры → фото → **2900мс** контур `fskGlow.glow` + **обводка вывески ФСК** (`#fskSign`) + имя `FSK_HERO_NAME=LEVEL_NAMES[2]` «Действующий предприниматель» (2★) → титул `#fskBrand` + **подслайд 1** (`fskShowSub(0)` — блоки по очереди). `fskIntroDone`.
3. **Подслайд 1** заголовок «Содействие кредитованию малого и среднего бизнеса Москвы» + 3 буллита: Получение финансирования · Подготовка документов · Поддержка бизнеса.
4. **Клик → подслайд 2** (`fskAdvanceSub`): блоки 1 уходят (fade) → пауза `CFG.reveal.textOut` → блоки 2 по очереди. Заголовок «Единый центр содействия кредитованию»: Кредитный брокеридж · Оценка кредитоспособности · Реструктуризация обязательств · Обучающие мероприятия.
5. **Клик → подслайд 3** (`fskAdvanceSub`): заголовок «Содействие кредитованию» (без акцента) — цифры: **24 550+** поручительств · **211 млрд ₽** портфель · **440 млрд ₽** привлечено + хайлайты «**5** программ льготного кредитования» / «**5** гарантийных продуктов» (строками).
6. **Клик → `fskStory`** (после последнего подслайда): текст+титул уходят (fade `fskFadeOut`) → **пауза 1000мс** → **level-up `runFskLevelUp` 2★→3★**, имя → `LEVEL_NAMES[3]` «Продвинутый предприниматель», баннер. **Аватар Купринова, реплика и exit-титул «ОТКРЫТИЕ КАФЕ» УБРАНЫ.** `fskStoryDone` через ~4.2с.
7. **Выход** (`fskToNext`, клик): `fskVeil` в чёрное → через 2700мс `cafeopen` (`coFromLanding=true`) **БЕЗ титула**; landing чёрный → раскрытие открывает кафе.

> Клик-хендлер fsk: `skipFskIntro` (не готово интро) → `fskAdvanceSub` (пока `fskStep<2`) → `fskStory` → `skipFskStory` → `fskToNext`.
> Функции подслайдов: `fskShowSub(i, instant)` (последовательно/мгновенно), `fskRevealSub` (построчно), `fskAdvanceSub` (fade→след.), `fskFadeOut` (плавно скрыть), `fskHideSubs` (жёстко скрыть). Скипы: `skipFskIntro`, `skipFskStory`.

## 12. cafeopen — Открытие кафе

Фон-база `cafe-2.png` (ремонт) → проявляется `cafe-3.png` (готово). **Ранг 3★ «Продвинутый предприниматель».** Авто-показ `runCafeOpen`:

1. **Вход** БЕЗ титула (раскрытие landing из чёрного); своя вуаль `coVeil` открыта. Задержка ~3900мс (ждём landing).
2. **Облако пыли** (`coDust`, 15 клубов `.puff`) поднимается. *(Мультяшные инструменты убраны — не вписывались в стиль.)*
3. @720мс: под пиком пыли проявляется `cafe-3` (`coBg2`) + вывеска **«Ваш любимый кофе»** (`coCoffee`).
4. @1800мс: пыль растворяется. @2100мс: **контур кафе** `coOutline` (прорисовка `coDraw` 2.4с) → @+2400 циан-пульс `glow`.
5. @4800мс: вывеска `coCoffee.lit` (циан-свечение). *(Обводка вывески ЦУБ — `coCub` — УБРАНА полностью: разметка, `const`, сброс, таймер.)*
6. @5400мс: **герой за столиком** `playCoHero` — искры → фото → 2900мс контур + печать имени `LEVEL_NAMES[3]` «Продвинутый предприниматель» (3★). `coHeroDone`.
7. **Клик → `coStory` (опасность):** контур кафе `glow→danger` + вывеска `lit→danger` (**оба → оранжевый** сигнал опасности) → пауза 1200мс → **ЭСКАВАТОР приезжает** (`coExcArrive`, точки/картинки из «Движение эскаватора.svg»):
   - 4 точки справа→влево (`CO_EXC_FRAMES`): дальняя (`excavator-arm-bucket.png`, маленький) искры→из размытия → @2200мс **оранжевый** неон (`co-exc.glow`); **красный аларм** `coAlarm` + **сирена** `#siren`;
   - средняя (`excavator-arm-up.png`, ковш поднят) → 3-я точка (`excavator_bucket_mid_alpha_tight.png`, ковш опущен) → ближняя = **паркинг**;
   - **ПАРКИНГ = НАЧАЛЬНАЯ ПОЗА ЭСКАВАТОРА СЛАЙДА 13** (`M3_EXC_FRAMES[0]`): `left:39.73% top:19.20% w:25.60%`, кадр `CO_EXC_PARK_SRC` = `excavator-arm-up.png` (**стрела поднята**; `m3-exc-pos2.png` — байт-в-байт тот же файл). Стык 12→13 без скачка. `--flip` не задаётся, поэтому `scaleX(1)` = `tf:'none'` на 13;
   - **работа ковшом** (`coExcWork`): **3 смены** `bucket↔up` + пыль (`coExcPuffBurst`, `.co-dust .puff`) + звёздочки (`coExcStars`, `.star`) у ковша (`PX/PY/VX/VY` пересчитаны под новый паркинг, **ПОДСТРОИТЬ**) → финальный кадр = `CO_EXC_PARK_SRC` + **аларм/сирена off** (`coExcDone`);
7.5. **ГЕРОЙ ВСТАЁТ** (`coPoseSwap`, после парковки эскаватора, ДО аватара): **одно движение, БЕЗ пыли**. Стоящий `coHero2`/`coHeroGlow2` (`hero-mission3.png`, координаты `#mission3 .m3-hero`) ставится в позу сидящего (`.seatpos`, без перехода) → `coPoseSet()` запускает встречное движение: сидящий `coHero`/`coHeroGlow` едет в позу стоящего (`.standpos`) и тает, стоящий приходит на своё место и проявляется. Обе пары анимируют `left/top/height/opacity` за **`--co-pose`=.9с** (`CO_POSE_MS=900` в JS — держать синхронно) → силуэт перетекает, подмены не видно. Панель имени едет туда же (`.co-name.stood`). Затем **пауза `CO_AVA_GAP`=400мс** (герой успевает «встать») → аватар, т.е. через `CO_POSE_MS+CO_AVA_GAP`=1300мс после парковки. *(Перед гашением снимаются `.reveal`/`.draw` — `roofFace`/`roofDraw` идут с `both` и держали бы `opacity:1`. Скип — `coPoseSetInstant()` в `skipCoStory`; `resetCafeOpen` возвращает сидячую позу без перехода.)*
8. **ПОСЛЕ смены позы** — аватар **Байкова** (`coBaikov`): `.live` @1933 → ФИО «Байков С. Н.» → реплика в **2 строки**: «**Угроза бизнесу!**» (красный `--alarm`, класс `.say-alarm`) + «Самое время мне вмешаться!» (белый). `CO_SAY` — массив сегментов `[{t,cls},…]`, `typeSay` печатает каждый сегмент в свой `<span>`; `\n` = перенос (`.say-txt{white-space:pre-line}`).
9. **АВТО после реплики** (без клика): **сначала РАСФОКУС всей сцены** (кроме аватара Байкова с ФИО и репликой) — включается слой `#coDefocusLayer` (`.scene-defocus.on`) → через 533мс exit-титул **«МИССИЯ 3 / ЗАЩИТА БИЗНЕСА»** (`coDangerTitle`, z9, `pop`) увеличением, чтобы не терялся на картинке. `coShowDangerTitle(instant)` в конце `coBaikov` (авто, с задержкой) и в `skipCoStory` (`true` — расфокус+титул сразу). Расфокус снимается в `resetCafeOpen`. Дальше **клик = переход** (авто-`coToNext` через 1600мс убран).

> **Механика расфокуса (общая со слайдом 14).** Слой `.scene-defocus` — `position:absolute; inset:0; z-index:7; backdrop-filter:blur(9px) brightness(.9)` (плавно через `transition`, класс `.on`). Лежит **над сценой (z<7), но ПОД аватаром (z8) и титулом (z9)**: `backdrop-filter` размывает весь фон стейджа + объекты позади РАЗОМ, аватар и титул сверху — резкие. Почему не блюр отдельных слоёв: (1) фон живёт на самом `.co-stage`/`.nc-stage` (это родитель — его `filter` не тронуть, не размыв аватар), а на 14-м покрывающего слоя типа `coBg2` вообще нет; (2) блюр каждого слоя даёт **резкую кромку по краям** (размытый край покрытия открывает резкий фон стейджа под ним). `backdrop-filter` берёт уже собранные пиксели — кромок нет.
10. **Выход** (`coToNext`): `coVeil` в чёрное (титул переживает) → через 2700мс `mission3` (`mission3FromLanding=true`), landing-титул «МИССИЯ 3» гаснет.
    **ФОКУС ВУАЛИ 12→13 — дорога под «Москва-Сити»** = точка старта скорой на 13 (`SPP_FOCUS` = центр `SPP_ANCHORS[0]`, `92.60% / 63.63%` стейджа). `coVeil` сворачивается в неё, `landingVeil` из неё же раскрывает сцену 13. Координаты переводятся из % стейджа в % окна через **`stageFocus()`** (стейдж letterbox-вписан, вуали — на весь экран). Прочие landing-переходы (`runLanding`, `fskToNext`) явно сбрасывают фокус в `VEIL_CENTER`, т.к. инлайн-стиль иначе протёк бы.

> Скипы: `skipCoIntro` (пыль+герой), `skipCoHero`, `skipCoStory` (оранжевое + эскаватор→паркинг через `skipCoExc` + аватар финал).
> Кадры эскаватора `CO_EXC_FRAMES` (взяты из SVG) и позиция пыли/звёзд в `coExcWork` (PX/PY/VX/VY) — **ПОДСТРОИТЬ по факту**.
> **Слайд 13 (danger) УДАЛЁН** — опасность-эскаватор перенесён сюда (на cafeopen). Функции `runDanger`/`excavatorDrive`/… остались в коде как dead-code.

## 13. mission3 — Миссия 3 (защита бизнеса)

Фон `cafe-3.png` (готовое кафе). **Ранг 3★ «Продвинутый» → по ходу 4★ «Опытный предприниматель».** Эскаватор — **2 положения** из «Миссия 3 - защита эскаваторы.svg» (viewBox 2172×724 = m3-stage): `M3_EXC_FRAMES` через `setM3ExcFrame(i)`: **поз.0** `m3-exc-pos2.png` (стрела поднята; rect 863/139) → **поз.1** `m3-exc-pos1.png` (ковш опущен, припаркован; rect с matrix — зеркально). Каждая картинка привязана к своему rect (свой аспект) — кадр меняется целиком (картинка+позиция). 5 шагов.

1. **Вход** (`mission3FromLanding` через landing): титул «МИССИЯ 3 / ЗАЩИТА БИЗНЕСА» (наследован от cafeopen) → раскрытие, титул гаснет; своя вуаль `m3Veil` держится открытой. Сразу после `resetMission3` вызывается **`m3PlaceAll()`** — **герой, эскаватор и скорая уже стоят на сцене**:
   - герой `m3Hero` + **циан-контур** `m3HeroGlow.glow` + **панель имени** `m3NamePanel` с готовым текстом «Продвинутый предприниматель» (3★) — **без печати**;
   - эскаватор `m3Exc` **поз.0** (стрела поднята) + **оранжевый неон** `.glow`;
   - скорая `m3Spp` на дальнем старте `SPP_START` (Москва-Сити) + **красный контур** `.glow` (мигалки `.live` и сирена — только при выезде).

   > **Механики «искры → фото из размытия → контур» больше нет** ни у одного из трёх. Искры (`m3Sparks`, `m3ExcSparks`, `m3SppSparks`) и `revealSound()` на слайде не играют; классы `.reveal` только снимаются в `resetMission3`.

2. **Кадр 1** (`m3Frame1`, через 1500мс): зажигается вывеска **«Ваш любимый кофе»** (`m3Coffee.lit`, циан). `m3Frame1Done`. **Авто:** через `M3_SPP_AT` (1200мс) → выезд скорой (БЕЗ клика). Скип `skipM3Frame1` — вывеска + выезд сразу.

3. **Кадр 2 + драка** (`m3PlaySpp`, **АВТО** после кадра 1; клик — ускорить): скорая **уже стоит** у Москва-Сити с контуром → включаются мигалки `b0/b1/b2` + **сирена**, сразу **непрерывный подъезд** `SPP_DRIVE=1900мс` (`m3Spp.animate` по 3 опорным точкам `SPP_ANCHORS`, offset кейфреймов по длине пути, `ease-in-out`; ступенек и пауз нет) → `onfinish`: доехала (`m3SppDone`, сирена смолкает) → **авто `m3Fight()`**: облако `m3Dust` (реюз `.co-dust/.puff`, зона эскаватор+скорая) + **тема Mortal Kombat** `fightSound()` (фрагмент `CFG.fight.from=5с`, длина `CFG.fight.dur=4.5с`, хвост гаснет за `CFG.fight.fade=900мс`) → под плотным облаком (`M3_EXC_PARK=1800мс`) эскаватор → **поз.1** (припаркован), **скорая ОСТАЁТСЯ** на месте (контур+мигалки) → пыль уходит (`M3_DUST_OUT=4800`) → `M3_SHTAB_AT=5100` шаг 4.
4. **Затемнение + текст Штаба** (`m3ShowShtab`, авто): `m3Dark` (`.m1-dark` градиент как слайд 9, `z-index:6` — приглушает всю сцену) фейдит + текст Штаба. **Формат текста = как на слайде 14 (`nc2Text`):** `m3Shtab` теперь `.fsk-slides.m1-text` = **заголовок** `.fsk-h.rin` + `.ch-list` из **4 блоков** `.ch-item` (иконка `.gw-ico` + текст с акцентом `.fsk-ac`). Появление построчное — `revealPod(m3Shtab, m3Timers)` (заголовок → блоки по одному, ритм слайда 11/14). **Сейчас текст = ЗАГЛУШКИ** («Заголовок Штаба – заглушка», «Пункт 1…4 – основной текст заглушки») — правильный добавит пользователь. `m3ShtabShown`.
5. **Клик → уровень** (`m3DoLevelUp`): текст+затемнение убрать → **`runLevelUp(m3HeroLU,{name:LEVEL_NAMES[4]})`** — баннer+салют+звезда в панель (3★→4★) + имя «Продвинутый»→«Опытный предприниматель». `m3LevelDone`. **Выход:** ещё клик → `newcafe` (мгновенный swap).

> Скипы: `skipM3Frame1` (тоже авто-запускает скорую), `skipM3ToShtab` (шаг 3→4 мгновенно: скорая в финале + эскаватор поз.1 + текст Штаба сразу целиком через `m3ShowShtab(true)` → `podRinAll`; `fightStop()` обрывает тему). На level-up/reset текст гаснет через `podRinHide(m3Shtab)` (fade блоков `.rin`), а не `.show`. Level-up-объект героя — `m3HeroLU` (панель `m3NamePanel`).

> **Звук драки** — файл `assets/audio/fight-theme.mp3` (тема Mortal Kombat), а не синтез: `fightPunch`/`fightBoing` удалены. `fightStop()` вызывается в `resetMission3` и `skipM3ToShtab` — иначе трек доигрывал бы на следующем слайде.

> **Тайминги хвоста завязаны на `M3_DUST_PD`** (длительность клуба пыли, 4350мс). По `@keyframes puff` клуб плотный между 18% и ~57% своей длительности; с разбросом `+(i%4)*170` (длительность) и `+(i%5)*60` (задержка) окно, где плотны ВСЕ клубы, = `[1084..2480]мс` → отсюда `M3_EXC_PARK=1800` (смена кадра эскаватора не видна). Облако полностью уходит к 5040мс → `M3_DUST_OUT=4800`, `M3_SHTAB_AT=5100`. **Меняешь `M3_DUST_PD` — пересчитай окно, иначе смена кадра эскаватора вылезет наружу.**

## 14. newcafe — МИССИЯ 4 (2 подслайда: часть 1 «нет пандуса» → часть 2)

Фон `cafe-3.png`. Геометрия из **«Миссия 4 - часть 1.svg»** (viewBox 2172×724 = `nc-stage`, `cafe-3.png` ровно 2172×724 → 1:1). Вход/выход — своя радиальная вуаль `ncVeil` (как `morebiz`). Цепочка **авто**, клик = скип.

**Подслайды:** `#ncStage` (часть 1) и `#nc2Stage` (часть 2, геометрия «Миссия 4 - часть 2.svg») — оба `.nc-stage`, видна ровно одна: `#ncStage.gone{display:none}` / `#nc2Stage.show{display:block}`. Счётчик `ncSub` (0/1), сброс в `resetNewCafe`.

**Слои (z):** вывеска `.co-coffee` 2 · **контур двери `#ncDoorOut` 2 (ПОД женщиной)** · женщина `.nc-woman` 3 · **герой `.nc-hero` 3** · контур мусорки `.nc-outline` 4 · **капли `.nc-drops` 4** · искры 5 · панель имени `.cafe-name` 6 · вуаль `ncVeil` 5 · титул `.cafe-quiz-title` 9.

1. **Вход** (`runNewCafe`): само-раскрытие из чёрного (`--veil-fast`). @900мс — **вывеска «Ваш любимый кофе»** (`ncCoffee`, `.co-coffee.show.lit`, циан-glow `coffeeGlow`, rect0 из SVG: 457/170 · 449×152, поворот 1.7°).
2. **Появление женщины И ГЕРОЯ — одновременно** (@1400мс), обоим механика «искра→фото→контур»:
   - `playNcWoman` — искры (`ncWomanSparks`) → фото из размытия (`--rv-photo` после `--rv-spark`) → @`CFG.hero.contour` **СИНИЙ неон-контур** (`.nc-woman.glow`, `--neon`, по альфе PNG). Звук `revealSound()` (даётся один раз, за обоих).
   - `playNcHero` — **герой поливает клумбу своего кафе** (`hero-water.png`, rect 436/391 · 150×266 из SVG; смотрит вправо, на женщину). Низ отсечён **`clip-path`** по ломаной из SVG (верх клумбы y≈543 + её правый край x≈538 вниз до y≈630) → ноги уходят за клумбу. `filter` применяется ДО `clip-path`, поэтому по срезу свечения нет. @`CFG.hero.contour` — синий контур + **включаются капли** + **панель имени** (`ncName`, шаблон `.cafe-name`, 4★ «Опытный предприниматель» = `LEVEL_NAMES[4]`, печать `CFG.type.name`; позиция 23.59%/55.5% — по центру над головой).
3. **Капли воды** (`ncDrops`, `.nc-drops.show`) — у носика лейки (ellipse 492.5/535.5 · rx6.5 ry17.5 из SVG → зона 22.38%/71.55% · 0.6%×4.83%). 5 капель `@keyframes ncDrop`, бесконечно, с разными `--del`/`--dur`. Идут **с появления героя до конца подслайда**.
5. **Движение к кафе** — `NC_WOMAN_ANCHORS` (3 опорные точки из SVG) + **WAAPI** (`ncWomanWalk`, `ncWomanKeyframes`): один непрерывный проход справа→налево вглубь (уменьшается), offset кейфрейма = доля пройденной ДЛИНЫ пути. `1028/451·162×264` → `869/442·144×235` → `752/434·119×194` (у входа). Синий контур сохраняется.
4. **Дошла** (`ncArrive`): **плач ребёнка** `babyCry()` (файл `assets/audio/baby-cry.mp3`, 7с, затухание вшито; стоп при уходе — `babyCryStop()`) + **КРАСНЫЙ неон** (`--alarm`): контур **входа в кафе** (`ncDoorOut`, 2 пути, под женщиной) + **мусорки** (`ncBinOut`) — прорисовка `draw` 1.2с → тревожный пульс `glow`. `ncWalkDone`.
6. **Аватар Кремневой Н. С.** (`ncKremneva`, **АВТО** @arrive+1900; клик — ускорить): искры→фото→скан (`avatar-kremneva.png`), `.live` @`CFG.hero.live` → ФИО @`CFG.hero.fio` → через 1200мс **реплика «Давайте делать больше чем бизнес!»**.
7. **РАСФОКУС сцены + блок «МИССИЯ 4 / БОЛЬШЕ ЧЕМ БИЗНЕС»** — после реплики включается слой `#ncDefocusLayer` (`.scene-defocus.on`, механика общая со слайдом 12 — см. там) → через 533мс титул `ncTitle` (`.mission-title.cafe-quiz-title`, z9) `pop`, чтобы не терялся на картинке. Аватар Кремневой (z8) остаётся резким. Расфокус снимается в `resetNewCafe`. `ncStoryDone`.
8. **Переход подслайд 1 → 2** (`ncToSub2`, клик) — **как со слайда 12 на 13, только внутри слайда**: `ncVeil` сворачивается в чёрное (`CFG.veil.toBlack`), титул z9 **переживает чёрное** → под чёрным меняется сцена (`ncStage.gone` + `nc2Stage.show`, `ncSub=1`) → пауза 900мс → `ncVeil` раскрывается (`--r:175%`) и **параллельно гаснет титул** (`.mission-title.pop.faded{opacity:0}`, `transition:opacity 1.1s`). `ncSubGo` — защита от двойного вызова.
9. **Подслайд 2** (`#nc2Stage`): та же сцена кафе, вывеска «Ваш любимый кофе» горит (`.co-coffee.show.lit`). *Контент из «Миссия 4 - часть 2.svg» (пандус + герой с шуруповёртом `hero-ramp.png` + мусорка) — **не наполнен**, только каркас.*
10. **Выход** (`newCafeToNext`, клик на подслайде 2): `ncVeil` в чёрное → `morebiz`.

> Скипы: `skipNcWalk` (вывеска+женщина у входа с синим контуром + красный неон, без плача; авто-запуск Кремневой), `skipNcStory` (аватар+ФИО+реплика+титул мгновенно).
> FSM клика: `ncSub===1` → `newCafeToNext` · `skipNcWalk` → `ncKremneva` → `skipNcStory` → `ncToSub2`.
> Вход в кафе и мусорка **уже есть на фоне** `cafe-3` — рисуем только неон-контуры поверх (пути 1-в-1 из SVG). Пульт: `CFG.newcafe{walk,hold}`, `CFG.baby{on,vol}`, токен `--nc-walk`.

## 15. morebiz — Больше чем бизнес

Фон — интерьер кафе `cafe - interior.png` (в нём уже есть незрячий гость с **меню Брайля**). **Титул и градиент затемнения УБРАНЫ** — градиент гасил правую часть под белый текст, а там теперь стоит герой. **Ранг на входе 4★**, на слайде level-up до **5★**.

**Слои (z внутри `.mb-stage`):** награда `.mb-award` 2 (при подсветке `.lit` → 8) · герой `.mb-hero` 3 · искры `.mb-hero-sparks` 5 · панель имени `.mb-name` 6 · затемнение `.mb-dark` 7 · контуры `.mb-out` (меню + протез) 8 · текст `.mb-text` 9.

**Геометрия (система 2172×724):**
- **герой** `hero-terminal.png` — за стойкой, пробивает оплату в терминале: `left 76.842% · top 28.315% · width 7.136%` (rect `1669/212 · 155×203` из «Миссия 4 - часть 3.svg»; **top = 205**). Панель имени — `left 80.410% · top 27.62%`.
  > Срез сажаем на **ЗАДНЮЮ кромку столешницы** (`y = 0.0294·x + 355.4` в пикселях фона) — за ней и скрывается стоящий позади человек. Передний кант (`y = 0.0558·x + 326.6`) проходит на 17px ниже: если сажать туда, герой «растёт из стойки». **Обрезка по линии столешницы ЗАПЕЧЕНА в PNG** (447×585); полный рост лежит рядом — `hero-terminal-full.png` (447×1459). Линия среза: `y = 0.0558·x + 326.6` в пикселях фона `cafe - interior.png`.
  `clip-path:polygon(0% 0%,100% 0%,100% 99.84%,0% 95.60%)` повторяет край файла — **обязателен**: свечение (`drop-shadow`) считается ДО обрезки, без clip-path неон обвёл бы диагональ среза каймой и герой «парил» бы над стойкой.
- **награда** `награда.png` — стеклянная табличка «социальное предприятие». Rect из «Миссия 4 - часть 3.svg»: `left 34.168% · top 69.404% · 8.15%×21.286%`, поворот `1.164°` (`transform-origin:0 0`).
- **меню** — контур `#mbMenuOut` по путям из того же SVG (`M94.5 454.5…` и `M99 483H142`). Пути **не замкнуты** (`Z` не ставить — иначе через меню проходит лишняя линия).
- **протез бариста** — контур `#mbArmOut`: один path из «Миссия 4 - часть 3.svg» (обведён вручную по силуэту руки с пальцами).

**Шаги (`mbStep`). Клик = скип текущего шага:**

1. **Вход** (`runMoreBiz`): своё раскрытие из чёрного (`morebizVeil`, `--veil-fast`). После вуали — `playMbHero()`: искры → фото из размытия → на `CFG.hero.contour` включается **синий неон-контур** (по альфе PNG, шаблон `.nc-hero`), **проявляется награда**, панель имени печатает «Опытный предприниматель».
2. **АВТО** (`mbHighlight`, через `contour + длина имени + CFG.hero.textGap`). Порядок важен:
   1. награда `.lit` + **контуры меню и протеза** прорисовываются (`draw`, +1.2с → `glow`; оба в массиве `mbOuts`). Герой **всё ещё в контуре**, сцена светлая.
   2. пауза `CFG.morebiz.textHold` — зритель разглядывает кадр.
   3. **гаснет контур героя и панель имени** (`.glow` / `.show` снимаются) — ПЕРЕД затемнением, а не вместе с подсветками.
   4. через `CFG.morebiz.dimGap` — затемнение `.mb-dark`, титул `#mbBrand` слева («Фонд поддержки / социальных / проектов», 3 строки), построчный текст через `revealPod`.

> **Титул ФПСП на слайдах 14 и 15** (`#nc2Brand`, `#mbBrand`): «Фонд поддержки / социальных / проектов» — **3 строки** через `<br>`, регистр даёт `.fsk-ttl{text-transform:uppercase}`. Кегль **штатный** `--sz-title` (3.1cqw), как на слайде 11. Ширины строк: 31.7 / 23.4 / 17.3cqw — все влезают в `max-width:36cqw`; правый край самой длинной — 37.7%, до колонки текста на 14-м (44%) зазор 6.3%.
3. **Клик** (`mbReward`): текст гаснет, затемнение и подсветки уходят → через `CFG.reveal.textOut` герою **возвращается контур и панель с именем** → ещё через `CFG.hero.textGap` идёт **level-up 4★→5★** «Московский предприниматель» (`runLevelUp`, контейнер `mbStage`). Готовность — по `levelHold(имя)`.
4. **Клик**: `moreBizToNext()` → сворачивание вуали → **binoc2**.

**Скипы:** `skipMbHero` (доиграть выход героя и сразу перейти к шагу 2) · `skipMbText` (`podRinAll` — весь текст разом) · `skipMbLevel` (мгновенно 5★ + новое имя, убрать баннер/звезду/салют).

## 16. binoc2 — Дубль слайда 10 (без разведки)

**Урезанная копия входа слайда 10.** Своего DOM нет — переиспользует общий `.stage` (карта), крышного героя и панель имени. Бинокль/зум/панораму/тур/финал-миссию сюда **не тянем**. **Ранг 5★ «Московский предприниматель»** (`BINOC2_HERO_LEVEL:5` — герой поднялся на слайде 15).

Общий вход вынесен в `runRoofHeroMapIntro(sceneId, lvl, onOpen)` — им же теперь пользуется слайд 10. Разница только в `onOpen`: слайд 10 запускает `runBinoc(RECON.binoc)`, дубль — ничего.

1. **Вход** (`runBinoc2`): тёмная карта, крышный герой `playRoofHero` (искры→фото→контур @2900). @2900мс — панель имени (5★ «Московский предприниматель»).
2. @4200мс (`CFG.hero.introHold`): `stage.dark→reveal`, `revealObject` всех объектов **разом** (под вуалью). @+3400мс: `reveal→open` — карта открыта, `binoc2Open=true`. **Дальше ничего не происходит.**
3. **Выход:** это ПОСЛЕДНИЙ слайд — переходов дальше нет. Клик во время интро → `skipBinoc2` (мгновенно раскрытая карта, без искр/вуали).

> `applyScene`: `binoc2` исключён из `blackout` (иначе стейдж чёрный), а `runBinoc2()` вызывается **после** общей ветки `resetBinoc()` — иначе она снесла бы его таймеры (`binocStartT`/`binocPanelT` общие с разведкой).

## Фоновая музыка (все слайды)

Четыре файла в `assets/audio/`: `bg-load.mp3` · `bg-city.mp3` · `bg-map.mp3` · `bg-cafe.m4a`. Пульт — `CFG.music{on,vol,xfade,loopTail}`.

| Трек | Слайды |
| --- | --- |
| `title` | 1 (волны, после 1-го клика) — **фрагмент 0–20с**, `BG_CLIP.title` |
| `load` | 1 (после 2-го клика — кнопка «Начать»), 2 |
| `city` | 3, 4, 7, 8, 11, 12, 13, 14 |
| `map` | 5, 6, 9, 10, 16 |
| `cafe` | 15 (разговоры в кафе) |
| — тишина | 1 (волны, до 1-го клика) |

`BG_CLIP[name] = {from, to}` — играть не весь файл, а фрагмент. Петля берётся от `to`, а не от конца файла; исходник не режем. Без записи в `BG_CLIP` трек играет целиком.

Карта сцен — `BG_SCENE` (сцена → имя трека). Один вызов `bgPlay(BG_SCENE[sid]||null)` в начале `applyScene()` — покрывает и `next()`, и `prev()`.

**Титула в `BG_SCENE` нет намеренно.** Стоял бы там — трек попытался бы стартовать ещё на волнах, и если браузер автоплей пропустит (политика зависит от истории посещений), музыка заиграла бы раньше времени. Вместо этого `bgPlay('load')` висит на `ccClose()` — 1-й клик по титулу. Гасится в `prev()` при возврате к волнам.

**Три правила, из которых всё следует:**

1. Слайды, делящие один трек, его **не перезапускают** (`bgPlay` на том же имени — no-op). Переход 3→4 или 7→8 не слышен, потому что перехода нет.
2. Трек **зациклен кроссфейдом сам в себя**: по `timeupdate`, за `CFG.music.loopTail` до конца, заводится вторая копия, первая гаснет. Время на слайде не ограничено, таймеров по слайду нет.
3. Смена трека — тот же кроссфейд (`CFG.music.xfade`), только на другой файл. Кривая **equal-power** (`sin`/`cos`), не линейная — иначе в середине перехода громкость проседает на ~3 dB.

> Почему не `<audio loop>`: mp3 хранит encoder delay и padding — на стыке петли слышна дырка.
> Почему не WebAudio `decodeAudioData`: ему нужен `fetch()`, а тот запрещён на `file://` (презентацию открывают и двойным кликом).
> Автоплей: браузер не пускает звук до жеста. Титул молчит, к слайду 2 клик уже был. Страховка — `bgKick()` на первом `pointerdown`/`keydown`.
